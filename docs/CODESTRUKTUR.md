# CI Sound Balancing Tool – Codestruktur

Eine HTML-Datei lädt eine Reihe JavaScript-Module in fester Reihenfolge.
Kein Framework, kein Build-Schritt, alle Variablen im globalen Scope.

> **Wartung dieser Datei**: bei jeder strukturellen Änderung am Code
> sofort mit aktualisieren. Insbesondere: neue oder gelöschte JS-Datei,
> neue zentrale Funktion, neue globale Variable, neuer Tab oder Sub-Tab,
> verschobener DOMContentLoaded-Handler. Aktualisierung gehört in
> denselben Arbeitsschritt wie die Code-Änderung, nicht nachträglich.

## Tabs und ihre Module

Anfragen kommen meist tab-orientiert. Diese Tabelle zeigt, wo der
Code des jeweiligen Tabs liegt.

| Tab-Beschriftung (DE) | data-tab ID | Hauptmodul(e) |
|---|---|---|
| Einführung | intro | HTML; `applyLang()` setzt Text und href des Manual-Links (`#introManualLink`) anhand `README_URLS` in i18n.js |
| Implantat | setup | ui-implant.js, freq-table.js |
| Messungen | messungen | mit drei Sub-Tabs (s.u.) |
| Meßergebnisse | ergebnisse | results.js, chart.js (mit Sub-Tabs) |
| Kurven | levels | levels.js |
| Schieber | schieber | levels-tab.js |
| Player | player | player.js |
| Laden/Speichern | file | file.js |
| Unterstützung | unterstuetzung | finanzen.js, unterstuetzung.js, unterstuetzung-graph.js |

Reihenfolge in `index.html` (`<button class="tab">`-Liste oben): Kurven
steht vor Schieber. Die `<panel-…>`-Divs darunter haben weiterhin die
historische Reihenfolge (schieber vor levels), das ist unkritisch — der
Inhalt ist DOM-ID-adressiert.

**Sub-Tabs in „Messungen"** (alle vier nutzen denselben Builder
`buildTestPanel` aus test-ui.js; Latenz seit BA 223):

| Sub-Tab-Beschriftung | data-subtab | Hauptmodul |
|---|---|---|
| Elektrodenlautstärke ausgleichen | test | test.js |
| Stereo-Balance | balance | lr-balance.js |
| Frequenzabgleich | freqmatch | freqmatch.js (Basis), freqmatch-adaptive.js, freqmatch-slider.js |
| Latenz | latenz | latency.js |

**Sub-Tabs in „Meßergebnisse"**:

| Sub-Tab-Beschriftung | data-subtab | Hauptmodul |
|---|---|---|
| Elektrodenlautstärke-Balance (Default) | results | results.js |
| Stereo-Balance | lrresults | results.js, lr-balance.js |
| Frequenzabgleich | freqmatch | results.js |
| Latenz | latenz | latency.js (Render), tab-print.js (Druck) |

Zentrale Verdrahtung aller Tabs (Tab-Wechsel, Tab-Sperre während
Test): tabs-eq.js (`switchTab`, `updateTabLockState`).

## Module im Ladeverlauf

Die Reihenfolge der Module ist fest. Module weiter unten dürfen
Funktionen aus Modulen weiter oben aufrufen. Top-Level-Code in einem
Modul (außerhalb von Funktionen und außerhalb DOMContentLoaded)
braucht seine Abhängigkeiten beim Laden – normale Funktionsaufrufe
erst zur Laufzeit.

**Einbindung in `index.html`:** Skripte und `style.css` werden nicht
mehr per statischer `<script src=…>`-/`<link>`-Tags geladen, sondern
über drei kleine Inline-Skript-Blöcke im `<head>`. Der **erste Block**
lädt synchron `js/version.js` mit `?t=<Date.now()>` (immer frisch,
ein kleiner Roundtrip). Damit ist `APP_VERSION` verfügbar, bevor der
dritte Block läuft. Der **zweite Block** (ab BA 83b) ist der
Konsolen-Fehler-Collector: er richtet `window._dbgErrors` (Array) ein,
patcht `console.error`, hört auf `window.error` und
`unhandledrejection` und zeigt bei Treffer einen roten Banner oberhalb
der Tab-Leiste an (→ `#dbgErrorBanner`). `window.error`- und
`unhandledrejection`-Events werden gefiltert: nur Dateien, die
`/js/` oder `index.html` im Pfad tragen, lösen den Banner aus.
Der Banner ist klickbar (öffnet das Debug-Panel via
`window.dbg.activate()`) und hat einen ×-Schließer. Mehrfach-Fehler
zählen im bestehenden Banner hoch. Der **dritte Block** schreibt
`<link>`- und `<script defer src=…>`-Tags per `document.write` in den
parser-laufenden HTML-Stream und versieht jede URL mit dem
Cachebuster-Parameter `?v=<APP_VERSION>`. Folge: Wenn die
Versionsnummer sich nicht ändert, dürfen Browser CSS und JS aus dem
Cache nehmen → schnell, akkuschonend. Bei jedem Versions-Bump in
`js/version.js` wechselt der Query-String → alle Dateien werden
frisch geholt. Bilder (favicon, Briefkopf-Logo) bekommen keinen
Cachebuster; sie werden über ihren Dateinamen versioniert (neuer
Inhalt = neuer Dateiname).

Zusätzlich stehen im `<head>` drei Meta-Tags (`Cache-Control:
no-cache, no-store, must-revalidate`, `Pragma: no-cache`, `Expires:
0`) gegen das Caching der HTML selbst. Auf Mobile werden die meist
ignoriert (gilt nur für echte HTTP-Header), schaden aber nicht.
GitHub Pages cached HTML ohnehin ~10 Min — daher kann die
Versionsnummer-Anhebung mit kurzer Verzögerung sichtbar werden,
danach läuft der oben beschriebene Mechanismus.

Frühere Variante mit `sessionStorage`-`cacheBust` (Wert =
`Date.now()`, persistiert pro Browser-Session) ist seit Bauanleitung
57 entfernt. Sie scheiterte auf Smartphones, weil Tabs dort tagelang
im Hintergrund leben und `sessionStorage` mit der alten Buster-Zahl
hängenbleibt → Updates wurden auf dem Handy oft nicht gezogen.

Wichtig zur Loader-Wahl: Per `document.write` eingefügte
`<script defer>`-Tags sind aus Browser-Sicht parser-eingefügt; `defer`
greift, sie laden parallel, werden in Dokumentreihenfolge ausgeführt
und blockieren `DOMContentLoaded`, bis alle ausgeführt sind. Eine
frühere Variante mit `document.createElement('script')` +
`appendChild` und `s.async = false` blockierte `DOMContentLoaded`
nicht (HTML5-Spec für dynamisch eingefügte Skripte): auf `file://`
fiel das nicht auf, weil das Dateisystem die Skripte praktisch instant
liefert; über HTTP (z.B. GitHub Pages) feuerte `DOMContentLoaded`
zwischen Loader und init.js, und sämtliche
`DOMContentLoaded`-Handler (init.js, test.js, lr-balance.js,
levels-tab.js, freqmatch.js) wurden nie aufgerufen → nur die zwei Tabs
mit hartcodiertem Text waren sichtbar, kein Click reagierte.

Die Reihenfolge der Module liegt als Array im Loader-Block in
`index.html` und muß bei neuen/entfernten Modulen dort gepflegt
werden. Die Loader-Liste enthält seit Bauanleitung 56 `js/`-Pfade
(z.B. `js/audio.js`).

Alle Logik-JS-Dateien liegen unter `js/`. Die Sprachdaten in
`i18n/de.js` etc. bleiben im Root-Ordner `i18n/` (kein `js/`-Prefix).

| #  | Datei | Inhalt |
|----|-------|--------|
| 0  | version.js | `APP_VERSION` — einzige Stelle für die Versionsnummer. Wird vom ersten Inline-Loader in `index.html` synchron mit `?t=<Date.now()>` geladen (nicht über die Loader-Liste). Bei jeder Bauanleitung auf `"2.<Bauanleitungsnummer>-beta"` hochsetzen, sonst zieht der Browser-Cache keine neuen Dateien. |
| 0a | debug.js | Debug-Panel (Bauanleitung 80/81/82). Globale API `window.dbg` mit `isActive`/`activate`/`deactivate`/`toggle`/`log`/`set`/`unset`/`test`/`runTest`/`runAllTests`/`flag`/`setFlag`. Aktivierung über Doppelklick auf `.brand-logo`, URL-Parameter `?debug=1` (oder `?debug=0` zum Deaktivieren) oder programmatisch. Status persistiert in `localStorage` unter `ciSb.debugActive`. Panel als rechte Sidebar (fixed, 360 px); im aktiven Zustand schiebt `body.dbg-active { padding-right: 360px }` den Hauptinhalt nach links. — Seit BA 81: Felder-Speicher getrennt in `_autoFields` (vom Polling befüllt) und `_userFields` (via `dbg.set(...)`); `_allFields()` liefert den Merge (user gewinnt bei Konflikt). Polling-basierter Feldsammler `_collectFields()` läuft per `setInterval(_, 500 ms)`, solange das Panel aktiv ist. Markdown-Snapshot in `_mdHeader`/`_mdFieldsBlock`/`_mdTestsBlock`/`_mdLogBlock` zerlegt; pro Sektion ein „kopieren"-Knopf kopiert nur diesen Block (inkl. Header). — Seit BA 82: Test-Registry `_testReg` (Map name → t), User-Auswahl `_testSelect` (Set), Flags `_flags` (persistiert in `localStorage` unter `ciSb.debugFlags`). Public API erweitert um `runTest(name)`, `runAllTests()`, `flag(name)`, `setFlag(name, v)`. Test-Funktionen können `true`, `false`, einen String oder `{ok, msg}` zurückgeben (oder eine Promise davon); Wrapper `_runTest` normalisiert. Default-Auswahl der Checkboxen über `_applyDefaultSelection` beim Aktivieren und bei `dbg.test(...)`-Registrierung: angehakt sind Tests mit `opts.tab === currentTab` oder `opts.tab === "global"`. Eigener DOMContentLoaded-Handler. Muß vor `mobile.js` geladen werden, damit andere Module früh `dbg.log`/`dbg.set` rufen können. |
| 0b | mobile.js | `IS_TOUCH_ONLY` (Touch-Erkennung per `matchMedia('(hover: none) and (pointer: coarse)')`), `safeFocus(el)` (focus-Aufruf nur auf Nicht-Touch-Geräten), `applyMobileReadonly(root)` (setzt `readonly` und `inputmode="numeric"` auf allen `input[type="number"]` im übergebenen Wurzelelement, no-op auf Desktop). Eigener DOMContentLoaded-Handler, der `applyMobileReadonly(document)` einmalig nach Page-Load aufruft. Wird von freq-table.js, levels.js, test-ui.js nach dynamischen Rebuilds erneut aufgerufen. |
| 0c | touch-ctrl.js | `attachLongPress(btn, onStep)` (Klick + Long-Press 400 ms initial / 100 ms repeat), `buildSliderTouchCtrl(slider, opts)` (Touch-Bedienleiste mit − / Fein / + / [Replay] direkt nach dem Slider, dispatcht `input`-Event auf den Slider), `buildStepperPair(opts)` (zwei Buttons mit Long-Press, Aufrufer hängt selbst ein). Kein eigener DOMContentLoaded-Handler; Aufrufer instanzieren wo gebraucht. |
| 1  | i18n.js | **Core:** leeres Übersetzungsobjekt `L = { de: {}, en: {}, fr: {}, es: {} }`, `lang`, `t()`, `applyLang()`, `updateMfrSelectLabels()`, Konstante `README_URLS`. Sprachdaten liegen in `i18n/de.js` … `i18n/es.js`, die danach geladen werden. **Seit BA 230**: `applyLang` unterstützt zusätzlich `data-t-title` — setzt `title`-Attribut (Tooltip) über `el.setAttribute('title', t(k))`; damit werden Beschreibungs-Tooltips der Ton-Buttons im Modal bei Sprachwechsel live nachgezogen. |
| 1a | i18n/de.js | Deutsche Übersetzungen — befüllt `L.de` via `Object.assign(L.de, { … })`. Muss nach `i18n.js` geladen werden (nutzt `L`). |
| 1b | i18n/en.js | Englische Übersetzungen — befüllt `L.en` via `Object.assign(L.en, { … })`. |
| 1c | i18n/fr.js | Französische Übersetzungen — befüllt `L.fr` via `Object.assign(L.fr, { … })`. |
| 1d | i18n/es.js | Spanische Übersetzungen — befüllt `L.es` via `Object.assign(L.es, { … })`. |
| 1e | dependency-lock.js | **Sperr-Schicht (b) Daten-vorhanden** (BA 149). Sperrt UI-Eingaben, wenn vorhandene Meßergebnisse durch eine Änderung ungültig würden. Sperr-Tabelle `DEP_LOCK_RULES` (Array, je Eintrag `selector` Single-Match **oder** `selectorAll` Multi-Match, `fieldLabelKey`, `getReasonKeys(el?)`). Funktionen: `depLockApply()` (iteriert die Tabelle; bei `selectorAll` querySelectorAll + forEach, bei `selector` querySelector; setzt/entfernt `.dep-locked` + `aria-disabled`), `_depLockLock(el, fieldLabelKey, reasonKeys)`, `_depLockUnlock(el)`, `depLockShowPopup(el)` (positioniert Popup `#depLockPopup` unter dem gesperrten Element), `depLockHidePopup()`. Globale Event-Listener (capture-Phase): `mousedown` (verhindert Dropdown-Öffnen bei `.dep-locked`, schließt Popup bei Außen-Klick), `touchstart` (analog für Touch), `keydown` (ESC schließt Popup, weitere Tasteneingaben auf `.dep-locked` werden verhindert). Kein eigener DOMContentLoaded-Handler — Event-Listener werden beim Skript-Load registriert. Drei Sperr-Schichten im Tool: (a) Test läuft → `lockTestTabs` in `test-ui.js`, (b) Daten vorhanden → dieses Modul, (c) Voraussetzungen fehlen → jeweils im Test-Modul. Sechs Regeln (BA 149/151/164/205): `#mfrSelect` (Hersteller), `#cfgSelect` (Hörtechnik), `.fo` (Hz-eigen, Multi-Selektor), `#refEl_freqmatch` (Referenzseite FreqMatch), `.ec-active` (Aktiv-Häkchen pro Elektrode, BA 164), `.ec` (Ausschluss-Checkbox, BA 205, eng nur auf adaptive FreqMatch-Trials). Zusätzlich BA 205: Transient-Popup-Helper `depLockShowTransientPopup(el, fieldKey, reasonKeys)` für selektive Sperren auf einzelnen Werten (z.B. `.ss="mute"`), bei denen das Element nicht dauerhaft gesperrt werden soll — temporäres Setzen der dataset-Attribute, `depLockShowPopup`-Aufruf, anschließendes Aufräumen. |
| 2  | core.js | `IMPLANTS`, `PROCESSORS`, `MFR`, `SIDES`, `PR_*`-Konstanten, `SII_THIRD_OCT`, `calc*`-Funktionen, `siiWeightsForFreqs`. Absolutmodus-Hilfsfunktionen: `LV_AXIS_MAX`, `lvAxisMaxFor`, `lvUnitLabelFor`, `dbFromMedel`, `dbFromCochlear`, `dbFromAB`. Frequenz-Hilfsfunktionen (Cent re 1000 Hz): `CENT_REF_HZ`, `hzToCent`, `centToHz`, `logInterpHz`, `meanCentStepOfFreqs`. Korrektur-Timestamp: `COCHLEAR_FAT_CORRECTION_DATE` (Date.UTC-Timestamp, 2026-05-31 UTC; markiert den Zeitpunkt der BA-136-Korrektur von `MFR.cochlear.freqs`). `MFR.cochlear.freqs` Position 9–21 in BA 136 korrigiert auf offizielle Cochlear-Standard-FAT-Werte (CI Select Manual, LFE 188 Hz, HFE 7938 Hz). |
| 3  | state-side.js | Globaler State (`sideData`, `activeSide`, `mfr`, `nEl`, `freqs`, `elFreqOwn`, `elSt`, `elNt`, `elExDur`, `elActive`, `manualLevels`, `refEl`, `bRes`, `config`, `presets`, `defaultMfr`, `toneType_freqmatch`, `toneType_test` (BA 246, Default `richCiHF`; eigene Persistenz für Elektrodenlautstärke-Test, analog `toneType_freqmatch`), `volume_freqmatch` (int 0..100, Default 75), `duration_freqmatch` (ms, Default 750), `pause_freqmatch` (ms, Default 400), `volume_test` (int 0..100, Default 75, BA 250), `duration_test` (ms, Default 750, BA 250), `pause_test` (ms, Default 300, BA 250), `toneType_balance` (BA 253, Default `richCiHF`; eigene Persistenz für Stereo-Balance, analog `toneType_test`), `volume_balance` (int 0..100, Default 75, BA 253), `duration_balance` (ms, Default 1000, BA 253), `pause_balance` (ms, Default 400, BA 253), `sequence_freqmatch` (Default `"ab"`, BA 254), `sequence_test` (Default `"ab"`, BA 254), `sequence_balance` (Default `"ab"`, BA 254), `slTarget_*`, `plSrcMeas`, `plSrcLevels`, `plSrcCurves`, `lvTabShowMeas`, `lvTabShowCurves`, `lvTabMode`, `lvTabVariant`, `plShowExperimental`, `plBalanceMode`, `plActiveSource` (`"music"`|`"sentences"`|`"noise"`|`"audiobook"`, Default `"music"`), `plAutoAdvance` (Auto-Weiter-Toggle, Default `false`), `plLoop` (Endlos-Toggle, Default `false`), `plShuffle` (Zufall-Toggle global, Default `false`, BA 258), `plPauseMs` (Pause zwischen Stücken ms, Default 2000), `plSentShowText` (Satz-Text-Einblender, Default `false`), `plNoiseSelectedId` (Default `"gen:pink"`), `plNoiseSortAxis` (Default `"kind"`), `plNoiseCategory` (Default `"_all"`, BA 262), `plNoiseSearchQuery` (Default `""`, BA 262), `pNoiseBuf` (dekodierter/generierter Geräusch-Buffer, Default `null`), `plSentBgEnabled` (Hintergrund-Geräusch Master-Toggle, Default `false`), `plSentBgItemId` (gewähltes Hintergrund-Geräusch, Default `"gen:pink"`), `plSentBgSnrDb` (SNR in dB, Default `0`), `plMusicSelectedId` (Default `null`), `plMusicSortAxis` (Default `"title"`), `plMusicCategory` (Default `"_all"`), `plMusicSearchQuery` (Default `""`), `audiologUserNote`, `userFileSuffix` (top-level, beide Seiten gemeinsam)). Pro Seite in `sideData[side]` zusätzlich: `fmVerfahren` (`'adaptive'`\|`'slider'`, Default `'adaptive'`; `fmMode` als Fallback beim Laden — BA 108), `fmAdaptiveDur` (Burst-Dauer ms, Default 400), `fmAdaptivePau` (Pause ms, Default 400) — Bauanleitung 02b/2. `freqmatchAdaptive` (Lauf-State für Pause/Resume: `{runs: [{runId, startedAt, completedAt, varSide, refSide, electrodeIdxList, tracks, roundQueue, startSigns, pairedToPrevious}], currentRunIdx, sliderEstimates: {[elIdx]: {cent, varSide, refSide, varFreq, timestamp}}}`, Default `null`). Seit BA 102: `sliderEstimates` enthält Vor-Schätzungen aus dem Slider-Modus (separat von `fRes`); wird von `_fmEnsureSliderStore` (freqmatch.js) angelegt und befüllt. Migration: `_fmMigrateAdaptive(fa)` verwirft alle älteren Speicherstände (Vor-`runs[]`-Format ohne `runs[]`-Array sowie das alte 2-Track-Schema mit `':up'`/`':down'`-Keys), erhält `sliderEstimates` jedoch auch bei runs[]-Verwurf; alle übrigen Mess-Datentypen bleiben erhalten. Zusätzlich `_fmCleanupLegacyFRes()` (entfernt nach dem `fRes`-Load in `file.js`/`init.js` Einträge mit alten Detail-Feldern `fmConvUp`/`fmConvDown`/`fmTrackDiff`/`fmStatusUpLast`/`fmStatusDownLast`). Seit BA 106: `_fmMigrateAltSliderFRes()` überführt Alt-Slider-fRes-Einträge (ohne `fmStatus`) in `sideData[varSide].freqmatchAdaptive.sliderEstimates` und entfernt sie aus `fRes`. Wird in `file.js` und `init.js` nach `_fmCleanupLegacyFRes()` aufgerufen. `_fmMigrateAdaptive` defaultet `fa.sliderEstimates = {}`, wenn das Feld fehlt. Side-Logik: `bindActiveSide`, `setActiveSide`, `withSide` (temporärer Side-Wechsel ohne UI-Update, für Druck/Export), `initSideData`, `loadSideData`. Konfig pro Seite: `setSideConfig`, `getFreqSource`, `syncFreqsToAcoustic`, `isSideUsable` (BA 155: `true` wenn Hörtechnik gesetzt und — bei CI — Hersteller gesetzt; Cross-Modul-Helper für Voraussetzungs-Sperren in `freqmatch.js`, `test.js`, `lr-balance.js`). Snapshot-Helpers (BA 156): `implantSnapshot()` (liefert `{left, right}` mit je `{config, manufacturer, nEl, deactivatedIdx[]}`), `implantSnapshotsDiffer(a, b)` (Vergleich zweier Snapshots), `renderSnapshotHint(testKey, containerEl)` (rendert Hinweis-Banner in `containerEl` wenn Snapshot von aktuellem Stand abweicht; `testKey` ∈ `{'lr', 'lat'}`). Player-Side: `getPlayerSide` (liefert "left"/"right"/"both"), `getPlayerBalance` (Inter-Ohr-Offset aus Mittelwert von `lrResults`), `getPlayerBalanceGains` (liefert {left, right} dB unter Berücksichtigung von `plBalanceMode`; respektiert `plApplyBalance`), `getRawBalanceGains` (wie `getPlayerBalanceGains`, aber ignoriert `plApplyBalance` — für Meßtests). UI-Helper: `updSideButtons`, `updFClearBtn`, `dEN`, `dENPrefix`, `effFreq`, `effFreqDisplay` (effFreq + Warp-Shift gemäß `pWarpOn`/`pWarpMode`/`pWarpStrength` und `fRes`; verwendet vom Kurven-Tab und Player für Anzeige und Frequenz-Interpolation — Audio-Pfad, Tests, Schieber, Meßergebnisse und MAPLAW nutzen weiterhin `effFreq`), `fRes`. Top-Level-Init am Dateiende. |
| 3a | audio-source.js | **BA 193 — generischer Audio-Quellen-Layer.** Wird nach `state-side.js`, vor `player.js` geladen. Provider-Registry: `amRegisterProvider(p)`, `amCollectItems(category)`. Deklarative Sortier-Achsen: `amSortAxesFor(category)`, `amSortItems(items, category, axisKey)`; `AM_SORT_AXES.geraeusche` enthält drei Achsen (`kind`, `spectrum`, `source`); `AM_SORT_AXES.saetze` (BA 197) vier Achsen (`lang`, `speaker`, `source`, `style`). Registrierte Provider: (1) `"generated"` — liefert drei generierte Standardrauscher (Weiß/Rosa/Braun) über `amGenerateNoiseBuffer(ctx, kind)` ohne Dateiladung; (2) `"embed"` — liest `window.CI_SB_EMBED.sources` aus dem Embed-Bundle (`assets/audio-embed/noises.js`). Alle Items haben einheitliches Schema: `{id, title, audio?, sourceTitle, license, credit, tags, _providerId}`. **BA 194 — Item-Buffer-Cache, RMS-Normalisierung und Pre-Mix:** `amGetItemBuffer(ctx, item)` (lädt/generiert AudioBuffer, cached unter item.id); `amGetNormalizedNoiseBuffer(ctx, item)` (liefert RMS-normierten Buffer unter `"norm:<id>"`, Kopie damit Original unverändert bleibt); `amMixForeground(ctx, fgKey, fgBuf, bgItem, bgBuf, snrDb)` (mischt Hintergrund sample-weise zum Vordergrund, Hintergrund wird geloopt, weiches Clipping bei ±1.0, LRU-Cache `_amMixCache` max 8 Einträge); `amMixCacheClear()` (leert Mix-Cache bei Toggle/Dropdown/SNR-Änderung). `_plNoiseDecodedCache` in `player.js` entfernt — Geräusch-Caching läuft jetzt über `amGetItemBuffer`. **BA 195 — Collection-Ebene + lokaler Hörbuch-Provider:** `AM_COLLECTION_SORT_AXES.hoerbuecher` (5 Achsen: `author`, `genre`, `lang`, `reader`, `title`); `amCollectionSortAxesFor(category)`, `amCollectCollections(category)` (sammelt Collections aus allen Providern, die `listCollections` implementieren), `amSortCollections(collections, category, axisKey)`. Lokaler Hörbuch-Provider `"local-books"`: `_amLocalBookCollections[]`, `amAddLocalBookCollection(col)`, `amRemoveLocalBookCollection(id)` (gibt Blob-URLs frei). **BA 196 — Webspace-Manifest-Loader:** `amManifestRoot()` (gibt `"audio.manifest/"` zurück — Manifeste werden same-origin aus dem Repo geladen); `AM_WEBSPACE_ROOT_DEFAULT`, `amWebspaceRoot()` (liest `window.CI_SB_WEBSPACE_ROOT`, Default `http://ci-sound-balancing.honigburg.de/opus/` — nur für Audio-URL-Auflösung); Zustands-Objekt `_amWebspace` (privat: `indexLoaded`, `failed`, `sources`, `loaded`, `pendingRefresh`); `amWebspaceLoadIndex()` (holt `audio.manifest/index.json` same-origin aus dem Repo), `amWebspaceLoadSource(srcKey)` (holt `source.json` + alle Kategorie-Manifest-JSON same-origin, ignoriert `kind:"index"`), `_amResolveAudioUrl(rawAudio, sourceBase)` (baut absolute Audio-URL aus Webspace-Root + base + Pfad). Provider `"webspace"`: `listItems` für `geraeusche/musik/saetze`, `listCollections` für `hoerbuecher`. Bootstrap: `amWebspaceBootstrap()` (wird in `init.js` nach DOMContentLoaded gerufen — lädt Index, dann pro Source parallel; ruft nach Erfolg `plNoiseRefreshUI`/`plSentBgRefreshUI`/`plBookRefreshUI` auf). Fix 196.1: Manifest-Fetches auf `amManifestRoot()` umgestellt (CORS-Problem für Manifeste entfällt). **BA 199 — Tag-Vererbung für Webspace-Items:** `_AM_COLLECTION_TAG_DEFAULTS` (Array: `lang`, `license`, `credit`, `url`), `_AM_SOURCE_TAG_DEFAULTS` (Array: `license`, `credit`); `_amBuildItemTags(item, col, source)` (baut zusammengefügtes `tags`-Objekt mit Vererbungs-Reihenfolge source-Top-Level < col-Top-Level < col.tags < item.tags). Webspace-Provider nutzt `_amBuildItemTags` in `listItems` und `listCollections`; `colTags`-Zwischenvariable entfernt. **BA 260 — Musik-Sortier-Achsen + lokaler File-Provider:** `AM_SORT_AXES.musik` (6 Achsen: `title`, `artist`, `album`, `genre`, `year`, `source`). Helfer: `amBucketsForAxis(category, axisKey, items)` (liefert sortierte Kategorie-Werte, Multi-Value-fähig für `genres`-Array), `amItemMatchesCategory(category, axisKey, cat, item)`. Lokaler File-Provider `"music-local-file"`: `_amMusicLocalFile`, `amMusicLocalSetFile(file)`, `amMusicLocalCurrent()`. **BA 261 — Musik-Ordner-Provider + Webspace-Anbindung:** Lokaler Ordner-Provider `"music-local-folder"`: `_amMusicLocalFolders` (Map cid→{id,label,files,items}), `_amMusicLocalNextId`, `_amMusicNewCid()`, `_amMusicBuildFolderItems(files, cid, folderName)`, `amMusicIngestLocalFolder(fileList)` (async; filtert Audiodateien, legt neue Sammlung an, gibt `{cid, count}` zurück), `amMusicListLocalFolders()`, `amMusicRemoveLocalFolder(cid)`, `amMusicResolveLocalFile(audioRef)` (löst `"local-music-folder:<cid>:<relPath>"`-Ref auf File-Objekt auf). `amWebspaceBootstrap` ruft jetzt `plMusicRefreshUI()` auf, wenn eine Source mit Kategorie `"musik"` geladen wird. |
| 3b | richtone-profiles.js | Auto-generiert von `tools/profiles_to_js.py` aus `docs/richtone_profiles.json` (BA 215). Globale Konstante `RICHTONE_PROFILES` mit 14 Instrumenten-Profilen (Acc, ASax, Bn, BTb, Cb, ClBb, Fl, Hn, Ob, Tbn, TpC, Va, Vc, Vn). Jedes Profil enthält `partials`, `vibratoHz`, `vibratoCents`, `amHz`, `amDepth`, `attackMs`. Wird in `index.html` vor `js/audio.js` geladen, sodaß `playRichToneProfile` in `audio.js` die Konstante nutzen kann. Bei JSON-Tunings das Generator-Skript neu laufen lassen, **diese Datei nicht von Hand bearbeiten**. |
| 3c | citest-profiles.js | Ergänzt `RICHTONE_PROFILES` um für CI-Messungen optimierte Profile. Basis-Profile: `CiH` (harmonisch, 5 ganzzahlige Partials 1, 1/2, 1/3, 1/4, 1/5) und `CiB` (leicht inharmonisch, Partial-Multiplikatoren 1, 2.005, 3.011, 4.019, 5.028 — Glocken-ähnlich), beide Attack 250 ms, Vibrato 5 Hz / 5-6 cent, AM 3.5 Hz / 8 %. Diagnose-/Vergleichsvarianten (3.2.238.2-beta): `CiHA` (Attack 600 ms, AM 18 %), `CiHS` (Attack 500 ms, AM 25 % bei 2,7 Hz — in der bei Martin beobachteten AGC-Wellenfrequenz), `CiHF` (Attack 500 ms, ohne AM — bei Martin der ruhigste Klang). Weitere Diagnose-Profile (3.2.239.2-beta) nach Nutzer-Test mit CiHF: `CiP` (harmonisch, kein AM, kein Vibrato — testet, ob Vibrato bei manchen CIs zusätzlich Welle erzeugt), `CiBF` (inharmonisch, kein AM, Vibrato bleibt — isoliert Inharmonik gegen AM-Wirkung). Gleiche Engine wie richtone-profiles.js (`playRichToneProfile`); toneType-Strings `richCiH`, `richCiB`, `richCiHA`, `richCiHS`, `richCiHF`, `richCiP`, `richCiBF`. Wird in `index.html` nach `richtone-profiles.js` und vor `audio.js` geladen. Anders als richtone-profiles.js ist diese Datei **von Hand gepflegt** — die Profile imitieren keine real existierenden Instrumente. |
| 4  | audio.js | AudioContext, `playTone(hz, vol, ms, ramp, toneType)`, `playSeq(eA, eB, off, opts)` (opts: `{toneType, aba}`), `gAC`, `dB2G`, `updInd`, `playRichToneProfile` (BA 215, Synthese aus RICHTONE_PROFILES-Eintrag: partials, Vibrato-LFO, AM-LFO, Profil-Attack). BA 225: globale Helfer `isValidToneType(tt)` (Whitelist + smplr:-Prefix-Check), `_playSmplrTone(c, hz, vol, ms, pan, token)` und neuer `smplr:`-Branch in `playToneTyped`. file.js und init.js nutzen `isValidToneType` statt eigener Arrays. **BA 242:** `playSweep` (und `sweepAct`-Variable) entfernt. **BA 243:** `playHold`, `corrG`, `playSingle`, `toggleHold` entfernt (toter Code nach Wegfall der freq-table Play/Hold-Spalten). |
| 5  | ui-implant.js | `buildImplantCard`, `updCochlearGen`. BA 143: onchange-Handler der globalen Parameter (implC, implIDR, implIIDR) rufen am Ende `validateImplantTable(activeSide)` auf, damit die Plausibilitätsprüfung sofort auf Änderungen reagiert. **BA 242:** Neue globale Funktionen `openImplantTonePopup`, `_implTonePopupUpdLabel`, `_implTpElectrodeFreqs`, `_implTpElectrodeLabels`, `_implTpDisabledElectrodes`. Modul-globale Variablen `_implTpCorrectVol`, `_implTpModalTone`. Öffnet `openToneSelectionDialog` mit Klavier-Widget (Hold: `playToneTyped`, 60 s max), Sweep-Modus und Korrektur-Toggles für den Implantat-Tab. |
| 6  | freq-table.js | `buildFreqTable`, `switchMfr`, `resetFreqs`, `actEl`, `allEl`, `allPairs`, `shuffle`, `randAB`, `gWt` |
| 6a | data/cochlear-fats.js | Cochlear Frequency Allocation Tables für Plausibilitätsprüfung. Globale Konstante `COCHLEAR_FATS` mit `standard_22_lfe188_hfe7938` als 22-Element-Array (Mittenfrequenzen in Hz, aufsteigend; entspricht E22..E1 in Cochlear-Notation). Quelle: CI Select App Manual S. 12/13. Wird ausschließlich vom Validator gelesen — Audio-Pfad und Berechnungen nutzen weiter `MFR.cochlear.freqs` aus `core.js`. Erweiterungen für alternative LFE/HFE und reduzierte Elektrodenzahlen folgen in BA 137. |
| 6b | implant-validate.js | Plausibilitätsprüfung User-Eingaben Implantat-Reiter. Aufruf-Schema: `validateImplantTable(side)` wird am Ende von `buildFreqTable` (freq-table.js) aufgerufen. Sammelt Warnungen aus internen `_implCheck…`-Funktionen (Schema `{level: 1|2|3, electrodeIdx?, field?: 'hz'|'thr'|'upper' oder Array davon, globalEl?: 'c'|'idr'|'iidr', messageKey, messageParams}`), markiert Eingabefelder per CSS-Klassen `impl-warn-red/orange/yellow` (strengste Stufe gewinnt, `_implApplyFieldLevel` iteriert über Array-`field`) und rendert die Liste in `#implValidateList`. Sprachsetzer `_implValidateApplyLang()` wird aus `i18n.js` aufgerufen. BA 133–137 implementiert: Hz-Monotonie (Level 1), Hz-Range (Level 1, Konstante `IMPL_VAL_HZ_RANGE` pro Hersteller), Hz-Größenordnung (Level 2, Faktor 5 gegen Default, `IMPL_VAL_HZ_MAGNITUDE_FACTOR`), Cochlear-FAT-Lookup (Level 2/3, `_implCheckHzCochlearLookup`, Schwellen `IMPL_VAL_HZ_TREND_YELLOW_CENT` 300 / `IMPL_VAL_HZ_TREND_ORANGE_CENT` 600, nutzt `COCHLEAR_FATS` aus `data/cochlear-fats.js`), Hz-Trend MED-EL/AB (Level 2/3, `_implCheckHzTrendMedelAb`, gleiche Schwellen 300/600, nur aktiv ab ≥3 User-Overrides, nutzt Helfer `_implLocalNeighborMedian` für lokalen Median über Nachbarn Fenster ±2 ohne i selbst), Hz-Sprung MED-EL/AB (Level 2/3, `_implCheckHzJumpMedelAb`, Schwellen `IMPL_VAL_HZ_JUMP_YELLOW_CENT` 400 / `IMPL_VAL_HZ_JUMP_ORANGE_CENT` 700, markiert rechte Elektrode des Paars). BA 138 ergänzt: THR/Upper-Wertebereich (`_implCheckThrUpperRange`, Level 1, Konstante `IMPL_VAL_THR_UPPER_RANGE` pro Hersteller: MED-EL 0–268.6 qu, Cochlear 0–255 CL, AB 0–1000 CU), THR≥Upper-Konflikt (`_implCheckThrUpperConflict`, Level 1, markiert beide Felder per Array-`field`), Größenordnung (`_implCheckThrUpperMagnitude`, Level 2, Faktor `IMPL_VAL_THR_UPPER_MAGNITUDE_FACTOR` 10, Aktivierung ab `IMPL_VAL_THR_UPPER_MIN_FOR_MAGNITUDE` 3 Werten), MAD-Ausreißer (`_implCheckThrUpperMAD`, Level 3, Faktor `IMPL_VAL_THR_UPPER_MAD_FACTOR` 3, Aktivierung ab `IMPL_VAL_THR_UPPER_MIN_FOR_MAD` 5 Werten; seit BA 140 ohne Status-Modifikation — nur `deactivated` wird ausgenommen). Neue Helfer: `_implThrOf`, `_implUpperOf`, `_implCollectColumnValues`, `_implMedian`, `_implMAD`. BA 141 ergänzt: FAT-Sonderprüfung bei Deaktivierung (`_implCheckFatOnDeactivation`, kein `electrodeIdx` → reine Box-Warnung). Auslöser ≥1 deaktivierte Elektrode. Sub-Tests: globaler Test (alle aktiven Elektroden haben Hz-eigen-Override) und lokaler Test (mind. ein direkter Nachbar einer deaktivierten Elektrode hat Override). Bestanden ↔ keine Warnung. Sonst: MED-EL/Cochlear Level 2 orange (`implValidateFatMissing`), AB Level 3 gelb (`implValidateFatAb`). BA 143 ergänzt: globale Implantat-Parameter — c-Wert (`_implCheckGlobalCWert`, MED-EL), IDR (`_implCheckGlobalIDR`, AB), IIDR (`_implCheckGlobalIIDR`, Cochlear). Konstanten `IMPL_VAL_GLOBAL_C/IDR/IIDR` mit `{ hardware, typical }`-Range (Level 1 rot bzw. Level 3 gelb). Schema-Erweiterung: `globalEl` (`'c'`/`'idr'`/`'iidr'`) markiert die globalen Felder `#implC`/`#implIDR`/`#implIIDR` via `_implGlobalSelector`. `_implApplyFieldLevel` zweigt jetzt zwischen `globalEl`-Pfad und `electrodeIdx`/`field`-Pfad; gemeinsamer innerer Helfer `_implApplyLevelToElement` markiert ein einzelnes DOM-Element mit der strengsten Stufe. |
| 7  | test-ui.js | Einheitliche Test-UI für die vier Sub-Tabs in „Messungen": `buildTestPanel`, `setTestExclConfirm`, `lockTestTabs`, `_mkEl`. Erzeugt drei Blöcke (Erklärungen, Voreinstellungen, Test) per Config-Objekt. `buildTestPanel` versteht zusätzlich `cfg.test.heightJudgment` (Antwort-Buttons höher/tiefer, `hidden` bis 02b/2), `cfg.test.statusGrid` (Per-Elektroden-Statusanzeige, `hidden` bis 02b/2) und `cfg.test.instructionKey` (optionaler i18n-Key für einen `<p class="test-instruction">`-Text, der zentriert über den Entscheidungsbuttons erscheint; jeder Test übergibt seinen eigenen Text, Aufrufer können ihn über `fmEls.instructionEl` zur Laufzeit austauschen); liefert im Result zusätzlich `hjContainer`, `hjHigher`, `hjLower`, `statusGrid`, `instructionEl`. **Seit testUI-Migration Schritt 1** (BA 107): neue API `buildTestPanel(parentEl, {id, explain, header, verfahren})` — interne Implementierung `_buildTestPanelNew`. Bausteine-Katalog: `pairIndicator` (variant `'electrode'`\|`'side'`\|`'token'`), `slider` (unit `'dB'`\|`'cent'`\|`'ms'`), `sliderValue`, `decisionButtons`, `confirmButton`, `actions`, `confidence`, `statusGrid`, `progress`, `cumulativeDisplay`, `instruction`, `keyHint`, `excludeButtons`, `applyButton`, `extraFragment`, `background` (ausklappbares Erklär-Akkordeon `<details>`, optionaler `titleKey` (Default `'testBackgroundTitle'`) + `bodyKey` i18n, `bodyAsHtml` bool; Verfahrens-Label immer automatisch angehängt — kein `noLabel` — BA 108/128). Lifecycle-Automatik: Tab-/Verfahren-Sperre zwischen Start und Stop, Pfeiltasten-Routing (←/→ Slider, ↑/↓ decisionButtons, Leertaste Replay, Z Undo, B Simul) mit automatischem Install/Remove beim Start/Stop, Aufleucht-Helfer (`.playing`-Klasse), Replay-Sperre (decisionButtons + confirmButton disabled während setPlaying≠null). Helfer-API `testUI.{pairIndicator.setLabels, pairIndicator.setPlaying, progress.set, statusGrid.setEntries, field.setEnabled, confidence.getValue, cumulativeDisplay.set}` — globales Objekt. Rückgabe der neuen API: `{id, explainBox, headerBox, testBox, exclOverlay, header: {verfahrenSelect, refSelect, volInput, durInput, pauseInput, seqSelect, targetSelect, startBtn, stopBtn, lockedHint, extraFragment}, verfahren: {<id>: {pairIndicator, progress, instruction, statusGrid, decisionButtons, actions, keyHint, slider, sliderValue, cumulativeDisplay, confirmButton, confidence, excludeButtons, applyButton, extraFragment}}}`. **Seit BA 109**: Feste Body-Reihenfolge (runningTitle → statusGrid → progress → pairIndicator → instruction → decisionButtons → actions → keyHint → slider → sliderValue → cumulativeDisplay → confirmButton → confidence → excludeButtons → applyButton → extraFragment → background → debugRun). `runningTitle` (testUI-intern, nicht cfg-konfigurierbar): `h3.test-running-title`, zeigt „TabTitel-Test 'Verfahren' läuft" während Test, hidden außerhalb. `verfahren-explain`-Info-Box (`div.info-box.verfahren-explain`) unter Verfahren-Dropdown (nur verfahren.length>1); Text aus `explainKey`-Feld pro Verfahren. Neuer Baustein `debugRun` (button.dbg-only, Hook `onDebugRun`). `B`-Tastatur-Shortcut entfällt; Simul-Button ohne `B`-kbd-Hint. cfg-Erweiterung: `explainKey`-Feld pro Verfahren. **Seit BA 110**: `statusGrid` an Position 16 (vor `background`) verschoben. `confidence`-Baustein aus neuer API entfernt (Render-Block in `_buildTestPanelNew` + `testUI.confidence`-Helfer); alte API behält beides. Pfeiltasten: `Z`→`Backspace` für Undo; `B` wieder als Simul-Shortcut. Undo-Button kbd-Hint `⌫`, Simul-Button kbd-Hint `B`. Header-Selects: Auto-Blur nach `change` (Capture-Phase auf `headerBox`). **Seit BA 111**: Body-Reihenfolge angepasst — `actions` (Zurück/Nochmal/Gleichzeitig) von Position 7 an Position 15 verschoben (nach `extraFragment`, vor `statusGrid`); `keyHint` bleibt auf Position 7 (direkt vor `slider`). `slider`-Baustein baut Touch-Buttons (`buildSliderTouchCtrl`) jetzt automatisch ein (cfg-Optionen `touchStep`, `touchFineStep`; kein `replay`-Argument); LS-Hint-Element (`ls-hint`) nur noch bei `unit === 'dB'` gerendert (bei cent/ms sind `lsHint*`-Refs `null`). `lr-balance.js` unangetastet. `latency.js` seit BA 223 auf neue API migriert. **Seit BA 113**: Slider-Bereich erweitert sich automatisch — neuer cfg-Vertrag `slider: { initialRange, maxRange, … }` ersetzt `ranges: [...]`/`defaultRange` (Rückwärts-Kompat: vorhandenes `ranges` wird gelesen als `initialRange = ranges[0]`, `maxRange = ranges[ranges.length-1]`; ohne `initialRange` greift kein Auto-Extend). Neue Modul-Hilfsfunktion `_maybeExtendSlider(slRef)` (definiert vor `_buildTestPanelNew`); wird bei `mouseup`/`touchend` am Slider und im Keydown-Pfeil-Handler nach `onSlide` aufgerufen; erhöht `input.min`/`max` um `initialRange`, sobald der Wert das aktuelle Limit erreicht, höchstens bis `maxRange`. Extend-Button (`btn.extend-btn`, i18n `bExtend`) und `refs.slider.extendBtn` ersatzlos entfernt. Neue testUI-API `testUI.slider.setValue(slRef, value)` setzt einen Wert und passt den Bereich auf das nötige `initialRange`-Vielfache zurück (Reset z. B. beim Elektroden-Wechsel). `refs.slider` enthält jetzt `{input, lsHint, lsHintBand, lsHintMark, lsHintLabel, unit, initialRange, maxRange, rangeIdx}` (kein `extendBtn`/`ranges` mehr). Keydown-Handler (`←/→`) liest das Limit aus `parseFloat(slRef.input.max)` statt aus `slRef.ranges[slRef.rangeIdx]`. CSS-Custom-Property `--sl-range-step` steuert die Track-Höhe (10 px Start, −0,8 px je Erweiterung, min. 4 px; Pseudo-Elemente `::-webkit-slider-runnable-track` und `::-moz-range-track` in `style.css`). **Seit BA 116**: IIFE `testUI.sideCheck` nach dem schließenden `};` des `testUI`-Objekts hinzugefügt. API: `testUI.sideCheck.run(cfg, onSuccess, onAbort)` (öffnet Seitenhörtest-Modal; `cfg = {sides: 'both'|'one', side: 'left'|'right'}`), `testUI.sideCheck.startIdleWatch(el, ms, onIdle)`, `testUI.sideCheck.stopIdleWatch()`. Modal-DOM (CSS-Klasse `sht-modal`) wird lazy beim ersten `run()`-Aufruf erzeugt; nutzt `_mkEl`, `gAC`, `playToneTyped` als Globals. Wird in BA 117 von `freqmatch.js` aufgerufen. **Seit 3.1.177-beta**: Helfer `testUI.verfahren.select(els, vId)` ergänzt (setzt `els.header.verfahrenSelect.value = vId` und feuert `change`); programmatischer Gegenpart zur manuellen Auswahl. Schließt eine Refactor-Lücke aus BA 129 — `fmSetVerfahren` rief diese API bereits auf, sie war aber nie implementiert; der Aufruf crashte beim JSON-Laden mit Adaptiv-Daten. **Seit 3.2.206.1-beta**: `_shtStartIdleWatch(el, ms, onIdle)` registriert Reset-Handler (pointerdown/keydown/click) auf `document` statt auf `el` — vorher landeten keydown-Events mit Fokus außerhalb von `el` nicht beim Capture-Listener, sodaß der Idle-Watch bei reinem Leertasten-Replay nicht zurückgesetzt wurde. Das `el`-Argument bleibt API-kompatibel, wird aber nicht mehr für Event-Registrierung benutzt. **Seit BA 207**: Neuer optionaler Header-Baustein `header.common.electrodeSelection` — generische Auswahl-Komponente: Header-Zeile mit Summary-Span + Button „Testelektroden auswählen", öffnet `_openElectrodeSelectionDialog` (globale Funktion, vor dem `testUI.sideCheck`-IIFE definiert). Konfiguration: `{ minSelected, getSelection, setSelection, getElectrodeStatus, electrodeLabel }`. Stumm geschaltete und ausgeschlossene Elektroden werden ausgegraut angezeigt, Mindestanzahl wird im Dialog erzwungen. Refs: `headerRefs.electrodeSelectionSummary`, `headerRefs.electrodeSelectionBtn`, `headerRefs.electrodeSelectionCfg`, `headerRefs.electrodeSelectionUpdate`. Aktuell nur von FreqMatch verwendet (`minSelected: 1`); für spätere Migration der Elektrodenlautstärke-Messung auf testUI vorgesehen (dort `minSelected: 2`). **Seit BA 209**: Neuer optionaler Header-Baustein `header.common.tonePopupButton` — per-Test-Tonart-Auswahl via Button + Modal-Dialog; `_toneTypeKey(tt)` (Helfer: mappt Tonart-String auf i18n-Key, definiert vor `_maybeExtendSlider`), `_openToneTypeDialog(cfg, onChange)` (globale Funktion, nach `_openElectrodeSelectionDialog`, vor dem `testUI.sideCheck`-IIFE). Konfiguration: `{ getToneType, setToneType, getVolume, getPreviewSequence }`. Der `toneType`-Dropdown ist seit BA 254 entfernt; `tonePopupButton` ist der einzige Tonart-Mechanismus. Probehör-Sequenz wird per `cfg.getPreviewSequence()` vom aufrufenden Modul geliefert (Array von `{hz, pan, durationMs}` und `{pauseMs}`-Steps). Refs: `headerRefs.tonePopupBtn`, `headerRefs.tonePopupUpdate`. **Seit BA 219**: Drei neue Helfer-Methoden im `testUI`-Namespace und `cfg.verfahren[].prerequisites`-Mechanik. `testUI.slider.setValueDisplay(slRef, text)` — setzt den Anzeigetext des `sliderValue`-Elements über `refs.slider.valueDisplay` (neu: `sliderValue`-Block in `_buildTestPanelNew` verlinkt `svEl` zusätzlich nach `refs.slider.valueDisplay`; `refs.sliderValue` bleibt für Rückwärtskompatibilität). `testUI.slider.setRangeHint(slRef, opts)` — positioniert Marker-Dreieck und Min/Max-Band unter dem Slider via `refs.slider.rangeHint`/`rangeHintBand`/`rangeHintMark`/`rangeHintLabel`; `opts.marker` Pflicht, `opts.min`/`opts.max` optional für Band, `null`/leer blendet aus. `testUI.explain` — neuer Namespace; `testUI.explain.setVisible(panelEls, id, visible)` schaltet Erklär-Absatz mit gegebener `id` im `panelEls.explainBox` per `hidden`-Attribut (No-op wenn nicht vorhanden). `cfg.verfahren[].prerequisites` — optionales Array von Voraussetzungs-Objekten `{checkFn, messageKey, titleKey, actions: [{labelKey, kind, run}]}`; `kind`: `'continue'` (Start läuft durch), `'abort'` (kein Start), `'custom'` (run() aufgerufen, Start bleibt aus). Interne Closure `_openPrereqDialog(prereq, onContinue)` in `_buildTestPanelNew`; neues Modal-DOM `.prereq-overlay` direkt nach `.exclude-confirm-overlay` im `parentEl`. Start-Button-Handler aufgeteilt: Closure `_doStartAfterPrereqs(vCfg2)` (bisherige Start-Sequenz) + Click-Handler iteriert `prerequisites` — erste verletzte Bedingung öffnet Dialog, weitere dieser Runde werden ignoriert; kein sichtbarer Verhaltensunterschied solange kein Verfahren `prerequisites` deklariert. **Seit BA 225**: `GROUPS` in `_openToneTypeDialog` um zwei Gruppen erweitert (`toneGroupSf2` mit 3 SF2-Keys, `toneGroupMellotron` mit 34 Variants). Render-Schleife mit Fallback für Items ohne i18nKey (zeigt letzten Token-Teil als Label). **Seit BA 227**: `_openToneTypeDialog` und Vorspiel-Helfer ausgelagert nach `js/tone-popup.js` (jetzt `openToneSelectionDialog`); `_toneTypeKey` und `tonePopupButton`-Header-Baustein bleiben in `test-ui.js`. Aufruf-Stelle (Z. 1018) verwendet die neue Funktion. **Seit BA 231**: `_toneTypeKey` löst `smplr:`-Tonarten zusätzlich über `window.toneTypeI18nKey` (aus `tone-popup.js`) auf, sodaß der Header-Button auch für Mellotron-Tonarten den korrekten i18n-Text zeigt. |
| 7a | tone-popup.js | Modaler Tonart-Auswahl-Dialog (`openToneSelectionDialog(cfg, onChange)`), aus `test-ui.js` ausgelagert in BA 227. Enthält die GROUPS-Konstante mit allen Tonart-Gruppen (Sinus, Komplex, Rich-Profile, Noise, Mellotron-Sampler), interne Helfer `_playPreview`, `_setToneButtonsDisabled`. **Seit BA 240**: Sanduhr-Konzept entfernt (`_setHourglassFor` und `btn-hourglass`-Spans weg, smplr-Lade-Branch in `_playPreview` entfernt). Hint-Box konfigurierbar via `cfg.hintKey` (i18n-Key; ohne Key keine Box). Vol/Dur/Pau-Felder in der Modal: aktivierbar via `cfg.showVolume/showDuration/showPause`; Getter/Setter `cfg.getVolumePercent/setVolumePercent/getDurationMs/setDurationMs/getPauseMs/setPauseMs`; Werte live zurückgeschrieben (kein OK-Bestätigen). i18n-Keys `tonePopupVolume/tonePopupDuration/tonePopupPause` (nur DE, Fallback für EN/FR/ES). Aktuell nur von `test-ui.js` Z. 1018 aus dem `tonePopupButton`-Header-Baustein aufgerufen; vorbereitet für künftige Aufrufer (Implantat-Tab, Mess-Verfahren). (BA 227) **Seit BA 228**: optionaler Klavier-Block oberhalb der GROUPS-Liste, gerendert wenn `cfg.keyboardMode` gesetzt. Modalbox kennt das Klavier nur abstrakt — `renderSamplerKeyboard` aus `js/sampler-keyboard.js` übernimmt die Render-Logik, Aufrufer-Logik (Anschlag-Verhalten) sitzt in `cfg.onPress`/`cfg.onRelease`/`cfg.getHighlightMs` (API umgestellt in BA 229). **Seit BA 230**: Auswahl-Items als Buttons statt Radiobuttons — pro Eintrag ein `button.tone-btn` in einer `div.tone-btn-row` (flex-wrap), Beschreibungstext als `title`-Tooltip (`data-t-title`), Vorspiel-Buttons ersatzlos entfernt (Klick auf Tonbutton spielt Burst-Sequenz selbst vor). Aktiver Button trägt Klasse `tone-btn--active`. `_setPlayButtonsDisabled` umbenannt in `_setToneButtonsDisabled`, Selektor `button.tone-btn`. Klavier-Bug-Fix (3.2.230.1): Tonbutton-Click-Handler ruft optional `cfg.onToneSelected(key)` auf; `close()` ruft optional `cfg.onModalClose()`; der Aufrufer (freqmatch.js) speichert die live-Tonart in `fmModalTone` und setzt sie in `onPress` ein (Fallback auf `toneType_freqmatch`). **Seit BA 231**: `GROUPS` in Datei-Scope gezogen; eine Mellotron-Sammelgruppe durch fünf Untergruppen nach Modell ersetzt (M300, Mark II, M400/Tron, Chamberlin, Mischbänke); alle 34 Items mit i18n-Keys versehen; globaler Helfer `window.toneTypeI18nKey(tt)` ergänzt — durchsucht GROUPS nach dem Sampler-Key und liefert den i18n-Key zurück. **Seit BA 239**: Zwei Closure-State-Flags `applyMeasLevels` und `applyBalance` (Default true) plus Toggle-Buttons (grün/grau analog Player, i18n `tonePopupApplyMeas`/`tonePopupApplyBalance`) zwischen Hinweis-Banner und Klavier-Block. Helfer `_tpMeasDbForStep(stepHz, stepPan)` (nutzt `withSide`/`compWLS`/`effFreq`/`elActive`/`elSt`/`elExDur`/`bRes`, Lookup auf nächste aktive Elektrode) und `_tpBalanceDbSym()` (Mittelwert von `lrResults`, fest symmetrisch). `_playPreview` multipliziert pro Step `effVol` mit den dB-Faktoren bevor `playToneTyped` aufgerufen wird; Latenz wird nicht angewandt (sequentielle Vorspielsequenzen). Ruft optional `cfg.onTogglesReady(corrector)` auf (nach Erstellen der Toggles, vor Klavier-Render) — `corrector(vol, hz, pan)` gibt korrigiertes vol zurück und liest `applyMeasLevels`/`applyBalance` live aus der Closure; ermöglicht Klavier-`onPress`-Korrektur ohne Zugriff auf Closure-Interna. |
| 7b | sampler-keyboard.js | Klavier-Widget für die Tonart-Modalbox: `renderSamplerKeyboard(container, opts)`. Tasten pro aktiver Elektrode mit Aufrufer-gelieferter Beschriftung (`opts.getElectrodeLabels`); zwischen je zwei weißen Tasten eine schwarze Zier-Taste auf dem geometrischen Mittel der Nachbarfrequenzen. Pointer-Events (pointerdown/up/cancel/leave). **Seit BA 229**: Zwei Modi, implizit aus den Callbacks abgeleitet — **Burst** (`opts.onPress(idx, hz)` + `opts.getHighlightMs() → ms`, Highlight läuft genau diese Dauer und springt dann zurück) und **Hold** (zusätzlich `opts.onRelease(idx, hz)`; Highlight an bei pointerdown, aus bei pointerup/cancel/leave, `setPointerCapture` falls verfügbar). Schwarze Zier-Tasten rufen mit `electrodeIdx === -1` und leuchten zusammen mit ihren beiden weißen Nachbartasten auf (Tasten-Referenzen werden beim Render in `whiteKeys[]`/`blackKeys[]` gesammelt, kein DOM-Selector zur Laufzeit). FreqMatch beschriftet via `dENPrefix() + dEN(i)` (Form „E1", „E2", …). Bei smplr-Tonart die noch nicht geladen ist: Lade-Hinweis „Lädt ..." (i18n `samplerKeyboardLoading`) über dem Klavier, stumm bis Sampler ready, weder `onPress` noch `onRelease` werden gefeuert. (BA 228, API umgestellt in BA 229) |
| 8  | test.js | `ROUND_ROBIN`-Tabelle, `compWLS`, Test-Logik für den Elektrodenlautstärke-Sub-Tab. Bindet sich an die von test-ui.js erzeugte UI (neue API `header`/`verfahren`). **Seit BA 247**: vollständige Migration auf neue testUI-API. Zwei Verfahren: `startTestFull` (Round Robin, persistentes Resume) und `startTestConv` (Konvergenz, immer frisch). Helfer `_testFilterByElectrodeSelection(pairs)` filtert Paar-Sequenz auf die im Header gewählten Elektroden. Globale Variablen: `_testActiveVerfahren` (ID des laufenden Verfahrens, `'full'`\|`'conv'`), `_testSelectedEls` (Elektroden-Array aus `electrodeSelection`-Header-Baustein). Hilfsfunktionen: `_testUpdateProgress` (Progress via `testUI.progress.set`), `_testUpdateRangeHint` (LS-Schätzband via `testUI.slider.setRangeHint`), `_testPlaySimul` (beide Töne gleichzeitig). **BA 250**: Audio-State-Helfer `tGVol()` (quadratische Lautstärkekonversion aus `volume_test`), `tGDur()` (aus `duration_test`), `tGPau()` (aus `pause_test`) — analog `fmGVol/Dur/Pau` in freqmatch.js. `curBase` ist immer 0 — Slider-Startwert ist der gespeicherte Offset (oder 0). Entfernt in BA 247: `startTest`, `showMode`, `recJdg`, `playManPair`, `afterManRes`, `selectiveElectrodes` + `_selectivePairsFromRR`/`_selectiveUpdateSummary`/`_selectiveOpenDialog`, `_testRstSlR`, `_testExtSlider`, `_testCheckExtend`, `_testUpdCumulative`, `_testUpdLsHint`, `TEST_SLIDER_RANGES`, `testSlRangeIdx`. Entfernt in BA 248: `_testRequestExcl`, `updFullSweepInfo`, `startTmr`/`stopTmr`/`updTmr`; Zustandsvariablen `testMode`, `compCnt`, `tStart`, `tInt` (in state-side.js). `init.js`-Keyboard-Handler angepasst: B→`_testPlaySimul()`, S→`_testSwap()`, Enter→`recBal()`, ArrowLeft/ArrowRight via `testUI.slider.setValue` auf `testEls.verfahren[_testActiveVerfahren].slider`. **Seit BA 252**: Klavier-Widget im Tonart-Popup. Neue Modul-Scope-Funktionen: `_testTpElectrodeFreqs()`, `_testTpElectrodeLabels()`, `_testTpDisabledElectrodes()` (Helfer analog `_implTp*` in `ui-implant.js` — eine Seite, alle Elektroden, abgewählte/ausgeschlossene als disabled-Index-Liste). Neue Modul-Scope-Variablen: `_testTpCorrectVol` (Korrekturfunktion aus `onTogglesReady`-Callback), `_testTpModalTone` (aktuell im Modal gewählter toneType, `null` wenn kein Modal offen). `tonePopupButton`-cfg erweitert um `onToneSelected`, `onModalClose`, `onTogglesReady`, `keyboardMode: true`, die vier Klavier-Callbacks und `onPress` (Vorhör-Burst auf aktiver Seite mit Korrektur via `_testTpCorrectVol`). |
| 9  | freqmatch.js | **Gemeinsame Basis** des Frequenzabgleich-Sub-Tabs (seit BA 129 aufgeteilt). Enthält: globalen State (`fmRunning`, `fmEls`, `fmRefSide`, `fmVarSide`, `fmSymmetric`, `fmVerfahren`, `fmSeq`, `fmSeqIdx`, `fmCurrentEl`, `fmCentOffset`, `fmFirstSide`, `fmIsPlay`, `fmPlayTO`, und alle Adaptiv-State-Variablen), Hilfsfunktionen (`fmCents`, `fmFreqFromCents`, `fmGVol/Dur/Pau/Aba`, `fmCorrGain`, `fmVarHz`, `fmDEN`, `fmBuildSeq`, `fmBuildSeqSymmetric`, `fmPrevCent`, `_fmInitSides`, `_fmEnsureSliderStore`, `_fmShouldOfferSliderEstimate`, `fmTrackKey/ParseTrackKey`), Audio (`fmPlayCurrent`, `fmPlaySimultaneous`), Persistenz (`_fmPersist`, `_fmMarkCompleted`, `_fmClearPersist`, `_fmTryRestore`, `fmRefreshResumeHint`, Timer-Funktionen), Shared Dispatchers (`fmUndo`, `fmAbort`, `fmFinish`), Infrastruktur (`_fmRequestExcl`, `fmHandleSlider`, `fmApplyLang`, `fmUpdateSliderModeAvail`, `fmSetVerfahren`, `fmLoadVerfahrenFromSide`). Cochlear-FAT-Hinweis: `_fmShouldShowCochlearFatHint` (prüft ob `fRes`-Einträge mit Cochlear + timestamp < `COCHLEAR_FAT_CORRECTION_DATE` vorhanden), `_fmRenderCochlearFatHint` (baut/entfernt DOM-Element `#fmCochlearFatHint` als `info-box info-box-warn` oben in `_fmParentEl`); Aufrufer: DOMContentLoaded und `fmApplyLang` (BA 136). **Seit BA 146**: Konfigurationsabhängige Tab-Zustände. Neue Funktionen: `_fmHasSliderEstimates` (prüft ob in `sideData[*].freqmatchAdaptive.sliderEstimates` mindestens ein Eintrag vorhanden ist — Gegenstück zu `_fmHasAdaptiveData` für Slider-Vor-Schätzungen; verwendet in `_fmAutoSetRefMode` und fmRCDlg-Bedingung), `_fmEvalTestEligibility` (gibt `{blocked, reason}` zurück — Gründe `'sideUnknown'` (BA 155, erste Prüfung via `isSideUsable`) / `'sideDeaf'` / `'bothAcoustic'`), `_fmAutoSetRefMode` (setzt `refSelect.value` automatisch auf die akustische Seite, nur wenn `fRes` leer und keine adaptiven Daten vorhanden), `_fmRenderHGWarning` (baut/entfernt `#fmHGWarning` als `info-box info-box-warn`, wenn eine Seite `config === 'hg'`), `_fmRenderBlockedWarning` (BA 173 entfernt — Sperre wird jetzt durch L1-Tab-Sperre (BA 172, bothAcoustic) und L2-Sub-Tab-Sperre (BA 173, sideDeaf) abgefangen; `fmBlocked_sideDeaf` und `fmBlocked_bothAcoustic` i18n-Keys entfernt), `_fmRefreshTabState` (zentraler Tab-Aktivierungs-Entry-Point: `_fmAutoSetRefMode` + `fmLoadVerfahrenFromSide` bei `!fmRunning`, dann alle Renderer — aufgerufen von `switchSubtab` in `tabs-eq.js`). `fmApplyLang` ergänzt um `_fmRenderHGWarning` (BA 173: `_fmRenderBlockedWarning`-Aufruf entfernt). In `tabs-eq.js`: toter `fmLoadModeFromSide`-Aufruf durch `_fmRefreshTabState` ersetzt. Eigener DOMContentLoaded-Handler. **Architektur-Konvention**: Pro Testverfahren eine eigene Datei; gemeinsame Infrastruktur bleibt in der Basis-Datei. Gilt für alle Messungen-Sub-Tabs: neue oder ersetzbare Verfahren bekommen eigene Dateien. Historische Beschreibung (bis BA 128): Frequenzabgleich-Test (Sub-Tab freqmatch). `fmStart`, `fmConfirm`, `fmAbort`, `fmApplyLang`, `fmPlayCurrent`, `fmCorrGain` (Korrektur-Gain pro Seite/Frequenz, interpoliert). Nutzt `getRawBalanceGains()` statt `getPlayerBalanceGains()` — Balance wird immer angewendet, unabhängig von `plApplyBalance`. Eigener DOMContentLoaded-Handler. Bindet sich an die von test-ui.js erzeugte UI. Globale Variable `fmMode` (`'slider'` \| `'adaptive'`, Default `'adaptive'`, Bauanleitung 02b/2). Modus-Switch-Funktionen: `fmApplyMode` (UI-Umschaltung), `fmSetMode` (Modus-Wechsel mit Zeitparameter-Umschaltung), `fmLoadModeFromSide` (Modus aus `sideData[varSide]` lesen). Seit Bauanleitung 74: Staircase-Kern liegt in `freqmatch-staircase.js`. Seit Bauanleitung 75: Trial-Loop vollständig implementiert — `fmStartAdaptive`, `fmNextAdaptiveTrial`, `fmPlayAdaptiveTrial`, `fmHandleHeight`, `fmReplayCurrent`, `fmFinishAdaptive`, `fmRenderStatusGrid`, `fmUpdateAdaptiveProgress`; interne Helfer `_fmConvertHeight`, `_fmWriteResult`, `_mkCell`, `fmEnableHeightButtons`, `fmDisableHeightButtons`, `setPlayingIndicator` (Ton-1/Ton-2-Leuchtindikator im adaptiven Test, BA85); globale Variablen `fmAdaptiveActive`, `fmAwaitingResponse`, `fmTracks`, `fmCurTrackId`, `fmCurFirstSide`, `fmTrialStartTs`. Konvergierte Tracks schreiben ihr Ergebnis in `fRes` (kompatibel zum Slider-Modus). Seit Bauanleitung 76: Pause/Resume-Persistenz — `_fmPersist` (Track-State in `sideData[varSide].freqmatchAdaptive` sichern), `_fmMarkCompleted` (completedAt setzen), `_fmClearPersist` (Lauf verwerfen, z. B. bei Referenzseite-Wechsel), `_fmTryRestore` (pausierten Lauf beim Start wiederherstellen), `fmRefreshResumeHint` (Start-Button-Label „Test starten" / „Test fortsetzen"). Seit Bauanleitung 77: Catch-Trials und not-perceivable — `_fmRemoveResult` (fRes-Eintrag bei not-perceivable entfernen); globale Variable `fmCurCatchInfo` (null \| `{direction, expectedResponse}` für laufenden Trial). Undo-Support: globale Variablen `_fmUndoSnapshot` (Track-State-Snapshot vor letzter Antwort) und `_fmNextTrialTO` (Timeout-Handle für Next-Trial, canceln bei Undo); Funktion `fmUndoAdaptive()` (stellt Track-State aus Snapshot wieder her, spielt Trial erneut ab); `fmUndo()` dispatcht im adaptiven Modus an `fmUndoAdaptive()`; Zurück-Button aktiv nach jeder Antwort, deaktiviert beim Start des nächsten Trials. Seit Bauanleitung 82: Lokalfunktion `_fmDbg(msg)` als Live-Log-Brücke ins Debug-Panel — schreibt nur, wenn `dbg.flag('adaptiv.live')` true ist. Hooks an acht Stellen: `fmStartAdaptive` (A), `fmNextAdaptiveTrial` (B), `fmHandleHeight` Response-Log (C) + Status-Wechsel-Log (D), `_fmWriteResult` (E), `_fmRemoveResult` (F), `fmFinishAdaptive` (G), `_fmPersist`/`_fmTryRestore` (H). Seit Bauanleitung 84: globale Variable `fmRoundQueue` (aktueller Round-Robin-State, wird von `fmPickNextTrack` in-place mutiert, in `_fmPersist`/`_fmTryRestore` mitgesichert); neue Funktion `fmComputeProgressStats(tracks)` (exportfähige Fortschritts-Statistik — wird auch von `renderFreqMatchResults` in `results.js` genutzt). Seit Bauanleitung 86: Debug-Simulation — globale Variablen `_fmSimActive` (bool), `_fmSimOffsets` (Map electrodeIdx→simulierter Wahrnehmungs-Offset in Cent); Funktionen `fmRunDebugSim` (startet Simulation, rollt Offsets lazy) und `_fmSimStep` (Polling-Schleife, ruft `fmHandleHeight` direkt auf); `fmPlayAdaptiveTrial` kehrt sofort zurück wenn `_fmSimActive`; `fmAbort` setzt `_fmSimActive = false`; Button „DEBUG: Testlauf" (Klasse `dbg-only`) wird am Ende von `parentEl` im DOMContentLoaded-Handler eingefügt — nur sichtbar bei aktivem Debug-Panel via CSS-Klasse `body.dbg-active .dbg-only`. **Aktuelles Schema (Bauanleitung 100, ersetzt BA 88/89/93/94/97):** Pro Elektrode pro Lauf genau **eine** Staircase mit 1-down-1-up-Regel (siehe `docs/spec/02b-freqmatch-adaptiv.md`); `freqmatchAdaptive` enthält `{runs: [...], currentRunIdx}`, jeder Lauf-Eintrag ist `{runId, startedAt, completedAt, varSide, refSide, electrodeIdxList, tracks, roundQueue, startSigns, pairedToPrevious}`. `fmTracks` und `runs[].tracks` haben String-Keys `String(electrodeIdx)`; Helfer `fmTrackKey(idx)` und `fmParseTrackKey(key)` für Key-Konvertierung. Globale Variable `fmCurPairedToPrevious` (gepaartes Bracketing — Lauf 2 invertiert die Startvorzeichen von Lauf 1 elektrodenweise; in `fmStartAdaptive` ermittelt, in `_fmPersist` ins Lauf-Objekt geschrieben). Funktion `_fmAggregateRunsForElectrode(side, idx)` liefert match-priorisierten Status und Werte `{cent, fmConv (σ_konv = Mittel der Track-Residuen), fmRunSpread (SD der Lauf-Matches), fmResiduum (sqrt(σ_konv² + σ_runSpread²)), fmStatusLast, runsCount}` zurück. `_fmWriteResult`/`_fmRemoveResult` schreiben fRes-Einträge mit `fmConv`/`fmRunSpread`/`fmResiduum`/`fmStatusLast`/`fmRunsCount`; alte Felder `fmConvUp`/`fmConvDown`/`fmTrackDiff`/`fmStatusUpLast`/`fmStatusDownLast` sind entfernt. `fmRenderStatusGrid` zeigt eine Zeile pro Elektrode aus dem Einzel-Track. `fmComputeProgressStats` zählt Tracks direkt (1 = 1 Elektrode). `fmCurTrackId` ist String (= `fmTrackKey(idx)`); globale Variable `fmLastPickedTrackId` für die Wiederholungs-Sperre im Restpool bleibt unverändert. **Seit BA 117**: `_fmHpPlayTone`, `_fmShowHpCheck` und `_fmHpEls` entfernt; Seitenhörtest-Dialog jetzt via `testUI.sideCheck.run()` (BA 116, test-ui.js). Neue Modulvariable `_fmParentEl` (in DOMContentLoaded gesetzt). Neue Funktion `_fmDoStartSlider` (eigentliche Slider-Startlogik nach SHT-Bestätigung, startet Idle-Watch). `fmStartSlider` und `fmStartAdaptive` rufen `testUI.sideCheck.run({sides:'one', side:fmVarSide}, callback, onAbort)` auf. Nach Teststart: `testUI.sideCheck.startIdleWatch(_fmParentEl, 5·60·1000, stopTest)` — Test wird nach 5 Minuten ohne Interaktion automatisch abgebrochen. `fmAbort` ruft `testUI.sideCheck.stopIdleWatch()` als erste Zeile. `fmSEBtnSkip` (Empfehlungs-Dialog) nutzt ebenfalls `testUI.sideCheck.run()`. **Seit 3.2.206.1-beta**: Neue gemeinsame Funktion `_fmStartIdleSideCheck()` ersetzt den direkten `startIdleWatch`-Aufruf in `_fmDoStartSlider` und `_fmDoStartAdaptive`. Nach 5 Min Idle wird `testUI.sideCheck.run({sides:'both'}, ...)` ausgelöst (Seitenabfrage statt stiller Test-Abbruch); bei Erfolg startet `_fmStartIdleSideCheck` sich selbst neu, bei Abbruch wird der Test gestoppt. Ursprünglicher Sinn des Timers (Seiten zwischendurch erneut prüfen) wiederhergestellt. Seit Bauanleitung 102: Slider-Modus reaktiviert als optionale Vor-Schätzung — `_fmEnsureSliderStore(side)` (legt `freqmatchAdaptive.sliderEstimates` an falls fehlend), `_fmShouldOfferSliderEstimate()` (prüft ob Empfehlungs-Dialog gezeigt werden soll), `fmUpdateSliderModeAvail()` (sperrt `slider`-Option im Dropdown sobald `runs.length >= 1`; ruft ggf. `fmSetMode('adaptive')` wenn aktueller Modus slider ist). `fmConfirm` schreibt jetzt in `freqmatchAdaptive.sliderEstimates`, nicht in `fRes`; `fmUndo` löscht aus `sliderEstimates`. Empfehlungs-Dialog `fmSliderEstimateDlg` (`fmEls.sliderEstimateDlg`) vor adaptivem Erststart — drei Buttons: Slider-Schätzung / Direkt adaptiv / Abbrechen. Seit Bauanleitung 104: `fmStartAdaptive` bestimmt den Track-Startoffset dreistufig — (a) Folgelauf-Bracketing aus `prevRun.perElectrode[idx].match ± FM_FOLLOWUP_BRACKET_OFFSET`, (b) Slider-Vor-Schätzung aus `_slStore[String(idx)].cent`, (c) klassisches `sign · 100` cent; lokale Variable `_slStore` = `sideData[fmVarSide].freqmatchAdaptive.sliderEstimates`. **Seit BA 108 (testUI-Migration Schritt 2):** `freqmatch.js` nutzt die neue testUI-API (`header`/`verfahren`-Schema). `fmApplyMode`, `fmHandleKey` und `setPlayingIndicator` entfallen; Lifecycle-Automatik (Tab-Sperre, Keyboard-Routing, Button-Zustände) vollständig durch testUI. `fmStart`/`_fmDoStart` entfallen — ersetzt durch `fmStartSlider` (onStart-Hook Slider) und `fmStartAdaptive` (onStart-Hook Adaptiv). `fmSetMode` → `fmSetVerfahren`; `fmLoadModeFromSide` → `fmLoadVerfahrenFromSide`; globale Variable `fmMode` → `fmVerfahren`. `fmUpdateSliderModeAvail` ruft jetzt `testUI.field.setEnabled(fmEls, 'verfahrenSelect.slider', …)`. Erklär-Akkordeon in beiden Verfahren via testUI-Baustein `background` (Titel automatisch „Wissenschaftliche Grundlage und Grenzen – [Verfahrensname]" via i18n-Key `testBackgroundTitle`; kein per-Verfahren `titleKey` oder `noLabel`). **Seit BA 109**: `fmPlaySimultaneous` (umbenannt aus `fmPlaySimul`) als `onSimul`-Hook im adaptiven Verfahren (beide Töne parallel auf ihren Seiten); `fmRunDebugSim` über `onDebugRun`-Hook im neuen `debugRun`-Baustein verdrahtet; manueller `_simBtn`-Block im DOMContentLoaded entfernt. `fmUpdateAdaptiveProgress` zeigt „Trial X von ca. Y" (Konstante `FM_TRIALS_PER_ELECTRODE_ESTIMATE` aus freqmatch-staircase.js). Timer-Tick reaktiviert: Funktionen `_fmStartTimer`/`_fmStopTimer`/`_fmTickTimer`; globale Variablen `_fmTimerInterval`, `_fmTimerStartTs`; Start in `_fmDoStartAdaptive`, Stop in `fmAbort` und `fmFinishAdaptive`. Slider-Verfahren: `'simul'` aus `actions` entfernt. **Seit BA 110**: Trial-Text-Render-Bug in `fmUpdateAdaptiveProgress` behoben — nutzt jetzt `testUI.progress.set`. AB/ABA-Flag (`fmGAba()`) in `fmPlayAdaptiveTrial` respektiert — dritter Ton (Wiederholung des ersten) mit Pause bei ABA. `confidence` aus `fmCfg.slider.body` und `fmLoadElectrode` entfernt. `fmCfg.verfahren`: Slider zuerst, Adaptive zweit. `fmLoadVerfahrenFromSide`: Default `slider` solange keine adaptiven Läufe; bei `runs.length >= 1` Default `adaptive`; gespeichertes `fmVerfahren` hat Vorrang wenn keine Läufe. `fmSliderEstimateDlg` (BA 102) unangetastet. **Seit BA 111**: Slider-Body-Bausteine ergänzt (`instruction`, `statusGrid`, `background`, `debugRun`); `actions` um `'simul'` erweitert. Neue Funktionen: `fmUpdateSliderProgress` (Fortschrittsbalken Slider-Modus), `fmRenderSliderStatusGrid` (Status-Tabelle mit Spalten Elektrode/Schätzung/Status), `fmRunSliderDebugSim` (Slider-Debug-Simulation mit Zufalls-cent), `_fmActiveProgress` (wählt je nach laufendem Verfahren den richtigen Progress-Ref für den Timer). `_fmTickTimer` nutzt `_fmActiveProgress()` statt hartkodiertem adaptivem Ref. `_fmStartTimer` jetzt auch in `fmStartSlider`; `_fmStopTimer` auch im Slider-Pfad von `fmAbort` und in `fmFinish`. `fmReplayCurrent` entfernt (keine Aufrufer mehr); `onReplay`-Hook in beiden Verfahren auf `fmPlayCurrent` umgestellt. Manueller `buildSliderTouchCtrl`-Aufruf im DOMContentLoaded entfernt — Touch-Buttons werden jetzt durch `_buildTestPanelNew` gebaut. Token-pairIndicator im Slider-Modus schreibt keine Hz-Texte mehr in die Labels (`fmShowElectrode` prüft `pi.variant !== 'token'`). **Seit BA 112**: `fmRenderSliderStatusGrid` zeigt 6 Spalten (Elektrode, Startwert Hz, Differenz cent, Differenz Hz, Schätzung mit Slider Hz, Status ✓/✗); aktuelle Elektrode mit CSS-Klasse `current-row` hervorgehoben; wird zusätzlich in `fmHandleSlider` aufgerufen — live-Update bei Slider-Bewegung. `fmRunSliderDebugSim` schreibt Zufallswerte −200…+500 cent. `fmStartAdaptive` Pfad (b): Slider-Vor-Schätzung versetzt um `sign · FM_INITIAL_START_OFFSET` (Schätzung als Mittelpunkt, Start 250 cent entfernt); Pfad (c): Default `sign · FM_INITIAL_START_OFFSET` (war `sign · 100`). Enter-Taste löst `onConfirm` aus wenn `confirmButton`-Baustein deklariert (in `test-ui.js`). **Seit BA 113**: Slider-Bereichs-Logik vollständig nach `test-ui.js` ausgelagert. Modul-Konstanten `FM_SLIDER_RANGES` und Modul-Variable `fmSlRangeIdx` entfernt. Funktionen `_fmCheckExtend` und `_fmExtendRange` entfernt; `fmHandleSlider` ruft sie nicht mehr auf. `fmShowElectrode` setzt den Slider via `testUI.slider.setValue(slRefs.slider, fmCentOffset)` (Bereich wird auf das nötige Minimum zurückgesetzt). `fmLoadElectrode` ohne `fmSlRangeIdx`-Reset und ohne `FM_SLIDER_RANGES`-While-Schleife. `fmStartSlider` ohne `fmSlRangeIdx = 0`. Externer `_slExtendBtn`-Listener im DOMContentLoaded-Handler entfernt. cfg `body.slider`: `ranges: [100, 500, 1200]` → `initialRange: 100, maxRange: 1200`. **Seit BA 147**: SYMMETRISCH-Modus Fundament. Neue globale Variable `fmSymmetric` (bool, `true` wenn `refSelect.value === 'symmetric'`). Neue Funktion `fmBuildSeqSymmetric()` (liefert `null` bei Index-Mismatch beider Seiten, sonst Array sortiert nach Durchschnitts-Mittenfrequenz). `_fmInitSides()`, `fmLoadVerfahrenFromSide()`, `fmUpdateSliderModeAvail()` um symmetric-Pfad erweitert. `_fmAutoSetRefMode()` setzt bei beidseitig CI automatisch `refSelect.value = 'symmetric'` (sofern Option vorhanden). In `test-ui.js`: `if (hc.refSelect.includeSymmetric)` fügt dritte Dropdown-Option `'symmetric'` (i18n `fmSymmetricOption`) an. In `freqmatch-slider.js` und `freqmatch-adaptive.js`: Stub-Alert bei symmetric-Start (Audio folgt in BA 148). In `freqmatch-adaptive.js`: `_fmDoStartAdaptive` nutzt jetzt `_fmInitSides()` statt manueller Side-Zuweisung. **Seit BA 160**: Dynamischer Intro-Text. Neue Funktionen: `_fmRenderIntroText` (setzt `#fmHintMethodPara` und `#fmHintWarnPara` je nach Hörtechnik-Kombination: `fmHintMethodCiNatural`/`fmHintMethodBothCI` für Methodentext, `fmHintWarn`/`fmHintWarnBothCI` für Referenzseiten-Hinweis; aktualisiert auch `dataset.t` für korrekte Sprach-Umschaltung), `_fmRenderPrereqHints` (zeigt/verbirgt seit 3.1.181 **zwei getrennte** LV-Paragraphen `#fmPrereqLvLeftPara` und `#fmPrereqLvRightPara`, jeweils via Helfer `_fmHasLvData(side)` aus `sideData[side].bRes`; zusätzlich `#fmPrereqSbHintPara` wenn `lrResults` leer; Texte stehen statisch in `data-t` der Paragraph-Config, nur Sichtbarkeit per `display`-Toggle). Beide Funktionen aufgerufen aus `fmApplyLang` und `_fmRefreshTabState`. Paragraph-Config in `explain.paragraphs` erweitert um diese zwei ID-tragenden Einträge ohne `key` (kein `data-t` → nicht von `applyLang` überschrieben). `test-ui.js` angepasst: `p.key` und `p.id` werden nur gesetzt wenn truthy. **Seit BA 207**: Neue State-Variable `freqmatchTestSelection: number[] | null` (`null` = Default „alle aktiven testen"); neue Funktionen `_fmFilterSeqBySelection(seq)` (Schnittmenge seq ∩ Selection, `null`-Selection = kein Filter; vor `fmBuildSeq` definiert), `_fmOnSelectionChanged()` (Sync-Dispatcher nach Auswahl-Änderung — ruft `_fmApplySelectionToTracks`/`_fmApplySelectionToSliderRun` auf, refresht Header-Summary, beendet Test bei leerer Sequenz). `fmBuildSeq` und `fmBuildSeqSymmetric` geben Ergebnis durch `_fmFilterSeqBySelection` zurück. `electrodeSelection`-Config in `fmCfg.header.common` konfiguriert die Header-Komponente aus test-ui.js BA 207. `freqmatchTestSelection` wird in `saveJson`/`loadJson`/`resetAll` in `file.js` mitgesichert. **Seit 3.2.230.1**: Neue State-Variable `fmModalTone` (null = kein Modal offen, sonst aktuell im Modal angeklickter toneType-String). `tonePopupButton`-cfg ergänzt um `onToneSelected(tt)` (setzt `fmModalTone = tt`) und `onModalClose()` (setzt `fmModalTone = null`); `onPress` liest `(fmModalTone !== null) ? fmModalTone : toneType_freqmatch`. **Seit BA 252**: Klavier-Widget Disabled-Logik umgestellt — `getElectrodeFreqs`/`getElectrodeLabels` filtern nicht mehr auf `elActive`, sondern liefern alle Elektroden bis `Math.min(vN, rN)`; neuer `getDisabledElectrodes`-Callback prüft var- UND ref-Seite (`elActive === false` oder `elExDur != null`). |
| 9b | freqmatch-staircase.js | Pure-Function-Kern für den adaptiven Frequenzabgleich. Methodik nach `docs/spec/02b-freqmatch-adaptiv.md`. Funktionen: `fmCreateTrack(electrodeIdx, startSign, startOffset?)` (Track-State; `startSign` ist `+1` oder `−1`; optionaler `startOffset` in cent überschreibt den Default `startSign · 100` — seit BA 104), `fmPickNextTrack(state, rng, lastPickedKey)` (erwartet `{tracks, roundQueue}`-State; 3. Parameter für Wiederholungs-Sperre im Restpool), `fmApplyResponse` (**1-down-1-up nach Levitt 1971** — bewegt nach jeder Antwort, halbiert Schrittweite bei Umkehrung), `fmComputeMatch` / `fmComputeResidual` (Mittel bzw. halbe Spanne der letzten 6 Umkehrungen — Konstante `FM_REVERSALS_WIN=6`), `fmTrackSummary`, `fmComputeProvisional(track)` (vorläufige Schätzung für aktive Tracks — Match ab 2 Umkehrungen, Residuum ab 4; wird von `fmRenderStatusGrid` und `_fmrBuildInProgressEntries` genutzt). Interne Helfer `_fmHalfStep`, `_fmCheckAndUpdateStatus`, `_fmMeanReversals`/`_fmHalfSpanReversals` (Match/Residual-Fallback bei Hard-Cap mit < 6 Reversals). Konstanten: `FM_STEP_SEQUENCE=[50,25,12,6,3]`, `FM_STEP_MIN=3`, `FM_REVERSALS_REQ=8` (Konvergenz-Schwelle), `FM_REVERSALS_WIN=6` (Match/Residuum-Fenster), `FM_RESIDUAL_OK=10` / `FM_RESIDUAL_FAIR=25` / `FM_RESIDUAL_WIDE=50` (abgestufte Konvergenz-Schwellen), `FM_TRIAL_CAP=80`, `FM_CATCH_INTERVAL=8`, `FM_CATCH_PHASE=5`, `FM_CATCH_MAGNITUDE=500`, `FM_NOT_PERC_MIN_CATCH=3`, `FM_NOT_PERC_ERR_RATE=2/3` (Catch-Fehlerrate für not-perceivable), `FM_PROVISIONAL_MATCH_MIN=2`, `FM_PROVISIONAL_RESID_MIN=4`, `FM_ANCHOR_SMALL_POOL=4`, `FM_FOLLOWUP_BRACKET_OFFSET=25` (Folgelauf-Bracketing-Offset in cent — BA 104), `FM_INITIAL_START_OFFSET=250` (Default-Cent-Abstand des ersten Trials — BA 112), `FM_CATCHUP_REVERSALS_THRESHOLD=2` (Catch-up-Priorisierung — BA 105), `FM_TRIALS_PER_ELECTRODE_ESTIMATE=25` (Schätzwert Trials pro Elektrode für Fortschrittsanzeige — BA 109). Seit BA 105: `fmPickNextTrack` ergänzt im Neuaufbau einer Round-Queue einen optionalen Bonus-Trial für einen zufällig gewählten Track mit `<FM_CATCHUP_REVERSALS_THRESHOLD` Umkehrungen, sofern mindestens ein anderer Track die Schwelle bereits überschritten hat. Anti-Wiederholungs-Sperre über Positionstausch im bereits geshuffelten Queue. Greift nur im normalen Round-Robin-Pfad, nicht im kleinen Pool (`<FM_ANCHOR_SMALL_POOL`). Status-Werte: `'active'` / `'converged'` / `'converged-fair'` / `'converged-wide'` / `'unstable'` / `'not-perceivable'` / `'aborted'` (der historische Wert `'converged-noisy'` existiert nicht mehr). Konvergenz: ≥8 Umkehrungen + Schrittweite am Minimum + Residuum unterhalb der jeweiligen Schwelle. Keine DOM-Abhängigkeit, kein DOMContentLoaded-Handler. |
| 9c | freqmatch-adaptive.js | Adaptives Testverfahren des Frequenzabgleich-Sub-Tabs (seit BA 129). Abhängigkeit: freqmatch.js. Enthält: `fmEnableHeightButtons`, `fmDisableHeightButtons`, `_fmApplySelectionToTracks` (BA 207: synchronisiert Track-Status `'active'↔'deselected'` mit `freqmatchTestSelection`; konvergierte/nicht-wahrnehmbare Tracks bleiben unangetastet), `fmStartAdaptive`, `_fmDoStartAdaptive`, `fmNextAdaptiveTrial`, `fmPlayAdaptiveTrial`, `fmHandleHeight`, `_fmConvertHeight`, `_fmAggregateRunsForElectrode`, `_fmRemoveResult`, `_fmWriteResult`, `fmFinishAdaptive`, `fmRenderStatusGrid`, `_mkCell`, `fmUpdateAdaptiveProgress`, `fmComputeProgressStats`, `fmUndoAdaptive`, `fmRunDebugSim`, `_fmSimStep`. Kein eigener DOMContentLoaded-Handler. |
| 9d | freqmatch-slider.js | **Slider Round**-Verfahren des Frequenzabgleich-Sub-Tabs (seit BA 129, Mehrfach-Runden seit BA 206). Abhängigkeit: freqmatch.js. Enthält: `fmUpdateSliderDisplay`, `fmShowElectrode`, `_fmUpdateSliderRangeMarker` (BA 206: Balken Min..Max + Median-Dreieck unter Slider), `_fmApplySelectionToSliderRun` (BA 207: synchronisiert `sliderRoundRun.electrodeIdxList`/`remainingInRound`/`fmSeq`/`fmSeqIdx` mit `freqmatchTestSelection` während laufendem Test), `fmStartSlider`, `_fmDoStartSlider`, `_fmSliderRoundEnsureRun` (BA 206: sliderRoundRun initialisieren/prüfen), `_fmSliderRoundLoadOrStartRound` (BA 206: Pause/Resume — bestehende Runde fortsetzen oder neue beginnen), `_fmShuffle` (Fisher-Yates für Runden-Reihenfolge), `fmLoadElectrode`, `fmConfirm` (schreibt in `rounds[]`-Historie + berechnet Aggregat), `fmSkip`, `fmPauseSlider` (BA 206: Lauf pausieren ohne Reset), `fmUpdateSliderProgress`, `fmRenderSliderStatusGrid`, `fmRunSliderDebugSim`. Lauf-State in `sideData[varSide].freqmatchAdaptive.sliderRoundRun` (`runId`, `currentRound`, `remainingInRound`, `completedInRound`, `electrodeIdxList`). Kein eigener DOMContentLoaded-Handler. |
| 10 | results.js | `renderResults`, `renderFreqMatchResults` (liest seit Bauanleitung 78 Adaptiv-Status aus `sideData.*.freqmatchAdaptive` — Status-Spalte + not-perceivable-Zeile; seit BA84 auch laufende Tracks als Zwischenstand via `_fmrBuildInProgressEntries`), `_fmrCollectNotPerceivable` (sammelt not-perceivable-Elektroden beider Seiten als `{side:elIdx → true}`-Map), `_fmrBuildInProgressEntries(side)` (BA84 — liefert Pseudo-fRes-Einträge für aktive Elektroden mit Status `in-progress`/`in-progress-early`, wird nur für Anzeige genutzt, schreibt nicht in `fRes`; seit BA85 delegiert die Schwellen-Logik an `fmComputeProvisional` aus freqmatch-staircase.js — `in-progress-early` nun <2 statt <4 Umkehrungen). Eigener DOMContentLoaded-Handler: bindet `fmrClearBtn` (löscht `fRes` + `freqmatchAdaptive` beider Seiten, ruft `renderFreqMatchResults`). Aktuelles Schema (BA 100): `_fmrCollectNotPerceivable` und `_fmrBuildInProgressEntries` lesen direkt den Einzel-Track je Elektrode (`run.tracks[fmTrackKey(idx)]`); Tabellen-Spalten **Konvergenz** (`fmConv`, i18n `fmrThConv`), **Lauf-Streuung** (`fmRunSpread`, SD der Lauf-Matches; i18n `fmrThRunSpread`), **Residuum** (`fmResiduum = sqrt(fmConv² + fmRunSpread²)`, i18n `fmrThResiduum`); In-Progress-Pseudo-fRes-Felder umbenannt zu `fmConv`/`fmRunSpread`/`fmResiduum`/`fmStatusLast`. Seit BA 103: `_fmrBuildSliderEntries(side)` als dritte Anzeige-Quelle (Slider-Vor-Schätzungen aus `freqmatchAdaptive.sliderEstimates`; Flag `_sliderEstimate: true`). `renderFreqMatchResults` baut `displayData` dreistufig: `fRes` > in-progress > slider-estimate (via `haveCovered`-Set). Status-Badge `slider-estimate` (`fm-badge-slider`, grau). Meta-Zeile zeigt Vor-Schätzungs-Zahl (`fmrSliderEstimateCount`). |
| 11 | chart.js | `drawDisabledBar` (Helper, auch von lr-balance.js genutzt), `_drawRefElLabel` (Helper, auch von player.js und print-md.js genutzt — zeichnet fettes „Ref.-El."-Label an der Referenzelektroden-Position), `buildLinearAxis(electrodes, padLeft, plotW, hzGetter?)` (gleichmäßige Elektroden-Verteilung über die Plot-Breite, elektrodennummern-basiert; liefert `hzArr` für die Hz-Beschriftung; verwendet von `drawChart` und `lrDrawChart` seit Bauanleitung 67), `buildCentAxis(electrodes, padLeft, plotW, hzGetter?)` (Cent-x-Achsen-Berechnung — seit Bauanleitung 67 nur noch im Kurven-Tab und in den Archiv-Druck-Renderern verwendet, nicht mehr in `drawChart`/`lrDrawChart`; optionaler `hzGetter`, Default ist `effFreq`), `_attachAxisTooltip` + `_axisTooltipHandler` (Hover-Tooltip über x-Achsen-Labels; zeigt **bedingt** Hz wenn `hit.hz != null` und Cent wenn `hit.cent != null`; nutzt `cv._axisHits` vom Caller, gemeinsame Tooltip-Div `#axisTooltip`), `drawChart` (Meßergebnisse, x-Achse elektrodennummern-basiert, Hz unter Achse), `drawFreqMatchChart(cv, fResData, opts)` (seit Bauanleitung 78 dritter Parameter `opts.notPerceivable` — Map `{side:idx → true}`; allEls-Einträge enthalten `isNotPerceivable`, `fmStatus`, `fmResidual`; zeichnet not-perceivable als rotes Quadrat mit ✕, Streuungs-Stati (`converged-fair`/`converged-wide`/`unstable`) mit orangenem Band; seit BA 103: `fmStatus === 'slider-estimate'` wird als hohle graue Raute mit gestricheltem Senkrechtstrich zur Nullinie gerendert; Tooltip zeigt „Vor-Schätzung: ±X ct"), `_fmcTooltipHandler` |
| 12 | file.js | `saveJson`, `loadJson`, `applyLoadedData`, `resetAll`, `exportEasyEffects`, `_safeUserFileSuffix`, `_applyUserFileSuffix`, `_migratePresetsFromIndexToFreq` (Migration alter Preset-Index-Werte auf Hz/Cent beim Laden). Seit BA 106: kein `fmStatus`-Filter mehr beim fRes-Load; stattdessen Sequenz `_fmCleanupLegacyFRes` (Alt-Adaptive-Schema) und `_fmMigrateAltSliderFRes` (Alt-Slider → Vor-Schätzung). Slider-Estimates über das `freqmatchAdaptive`-Sub-Objekt automatisch mitgesichert. |
| 12b | print.js | Druck-Infrastruktur: `buildPrintHeader` (Mini-Kopf für Einzelausdrucke; bindet Logo `assets/images/logo_briefkopf6.png` rechts oben ein, Höhe 150 px, Flexbox-Layout mit Titel+Subtitle links, Logo rechts), `openPrintWindow` (neues Fenster, HTML schreiben, drucken), `canvasToImg` (Canvas → `<img>` PNG-Daten-URL). Wird von den Tab-spezifischen Druck-Handlern in den jeweiligen Tab-Modulen aufgerufen. Der zentrale „Alles drucken"-Button (`fPrintBtn` in init.js) ist unabhängig davon. |
| 12d | print-md.js | Markdown-Generatoren für Archiv-Box (Modus A) und Audiologen-Box (Modus B). Modus A: Datensammler `collectArchivData` plus Renderer `renderArchivMarkdown` und Sektion-Helfer `_archivMd*`. Interne Sammler-Helfer: `_collectTestSettings` (BA 254; pro Test je Tonart/Tonfolge/Vol/Dur/Pau), `_collectSideData`, `_collectBilateral`, `_collectPlayer`, `_collectSaetze`, `_pickUpperLevel`, `_calcAbsDelta`. Druck-HTML: `renderArchivPrintHtml` (wandelt Markdown via `_mdToHtmlBasic` in HTML, injiziert PNG-Grafiken und bindet Logo `assets/images/logo_briefkopf6.png` per `float: right` am Body-Anfang ein — kein `buildPrintHeader`-Pfad), `_archivInjectInserts` (HTML-Injektor per H2/H3-Anker), sechs Canvas-Renderer `_archivChartLoudness`, `_archivChartSchieber`, `_archivChartKurven`, `_archivChartFreqmatch`, `_archivChartLR`, `_archivChartPlayerEq`. `_archivChartKurven` und `_archivChartPlayerEq` nutzen `buildCentAxis` mit `effFreqDisplay` als hzGetter (cent-skaliert, Warp-bewußt analog zur GUI seit Bauanleitung 71). `_archivChartLoudness` und `_archivChartLR` nutzen `buildLinearAxis` (elektrodennummern-basiert mit Hz unter Achse analog zur GUI seit BA 67/71). `_archivChartSchieber` nutzt ebenfalls `buildLinearAxis`, zeichnet aber nur die Elektroden-Bezeichnung (analog zur GUI seit BA 66/71). `_archivChartFreqmatch` hat seit jeher eine log-Hz-Achse. Zeichenhelfer `_archivMkCanvas`, `_archivDrawAxis`, `_archivDrawElCentLabel` (dreizeilig E / Hz / ¢ — verwendet von Kurven- und Player-EQ-Druck-Renderern auf cent-skalierter Achse), `_archivDrawElHzLabel` (zweizeilig E / Hz — verwendet von Loudness- und LR-Druck-Renderern auf elektrodennummern-basierter Achse seit Bauanleitung 71), `_archivDrawElLabel` (einzeilig nur E — verwendet vom Schieber-Druck-Renderer seit Bauanleitung 71), Konstanten `_ARCHIV_CHART_W`/`_H`. Modus B: `buildAudiologMarkdown`, `audiologPrint`, `mdAudiologFilename`, Helfer `_audiologMainSides`, `_audiologSideLabel`, `_audiologDbForSide`, `_audiologResForSide`, `_audiologAbsDelta`, `_audiologLoudnessTable`, `_audiologFmRowsForSide(side)` (modus-bewußte Helper: liefert pro Elektrode auf `side` die effektive Frequenzabgleich-Zeile via `buildWarpPoints` + `centShift` — exakt die Player-Audio-Mathematik. Im sym-Modus hat jede Seite je cent/2; im left/right-Modus hat nur die jeweilige Seite Werte (seit BA 178; alte Bezeichner `var_side`/`ref_side` migriert von `_migrateLegacyWarpMode` aus Alt-Saves); seit BA 103: Row-Objekte enthalten `_sliderEstimate`-Flag aus `sliderByEl`), `_audiologFreqTable(side)` (seit BA 103: Slider-Estimates mit `†` markiert, Fußnotenhinweis `audFmSliderEstNote`), `_audiologMaplawSection`, `_audiologBalanceBlock`, `_audiologLatencyBlock`, `_audiologTestProgramHint`, `_audiologIsTestProgram`, `_audiologMissingImplantData`, `_audiologAdvice`, `_audiologLastMeas`, `_audiologConfigLabel`, `_audiologChartImg` (2×-Auflösung, Residuum-Fehlerbalken), `_audiologFreqChartImg(side)` (log-Hz × Cent-Punktdiagramm für Frequenzabgleich-Sektion. Datenquelle ist `_audiologFmRowsForSide(side)` — modus-bewußte effektive Cent-Verschiebung. Grün/rot je Cent-Vorzeichen mit Elektrodenlabel, offene Kreise für vorläufige Punkte aus laufendem Test analog `_archivChartFreqmatch`; seit BA 103: Slider-Estimates als graue Raute; bei sym-Warp + einseitigem Druck wird er pro Seite zweimal mit unterschiedlichem `side`-Argument aufgerufen — siehe Druck-Pfad), Mini-MD→HTML-Konverter `_mdToHtmlBasic`. Entfernte i18n-Schlüssel: `audiologRequestsBody`, `audiologSecRequests`, `audiologSecUserNote`, `audColHzOld`, `audColHzNew`, `audiologToolVersion`. Neue i18n-Schlüssel: `audiologToolVersionLine`, `audColHzDefault`, `audColHzManual`, `audColHzWish`, `audMissThr`, `audMissFreqOwn`, `audiologMissingIntro`. Gemeinsame Helfer: `mdCopyToClipboard`, `mdDownload`, `mdArchivFilename`, `mdDateStampFile`, `_mdEsc`, `_mdFmtDb`, `_mdFmtHz`, `_mdBilateralLabel`. Lädt zwischen `print.js` und `tab-print.js`. |
| 12c | tab-print.js | Tab-spezifische Druck-Funktionen: `printImplantTab` (Implantat-Tab), `printErgebnisseTab` (Dispatcher Meßergebnisse-Sub-Tabs), `_printResLoudness`, `_printResLR`, `_printResFreqmatch`, `_printResLatency`, `_printCloneSafe` (DOM-Klon mit Canvas→img-Ersatz), `printKurvenTab` (Kurven-Tab, Chart-Card + Kurvenfunktionen-Card), `_buildPresetCardPrint` (datengetriebene Preset-Tabelle für Druck: nur aktive Kurven, Werte als Text), `printSchieberTab` (Schieber-Tab: Canvas-Bild + Werte-Tabelle pro Elektrode, im Absolutmodus mit Hersteller-Einheit-Spalte). Nutzen die Helper aus `print.js`. |
| 13 | tabs-eq.js | `switchTab`, `_switchTabInternal` (BA 172: interner Tab-Wechsel ohne Sperr-Check — enthält den kompletten alten switchTab-Body ab Klassen-Toggle), `switchSubtab`, `_switchSubtabInternal` (BA 173: interner Sub-Tab-Wechsel ohne Sperr-Check — enthält den kompletten alten switchSubtab-Body ab Subtab-Button-Toggle), `updateTabLockState`, `updPlSrcButtons`, `updEqToggleBtn`, `updBalApplyBtn`, `updLatApplyBtn`. **BA 172 — Tab-Sperre L1:** Konstante `LOCKED_TABS_L1` (Array der fünf gesperrten Tabs: messungen/ergebnisse/levels/schieber/player). Globale Variable `window._tabLockState` (`{locked, reason}`). Funktionen: `evalTabLockState()` (bewertet Sperr-Zustand anhand `sideData` — Reasons: `"unconfigured"`, `"bothAcoustic"`, `null`), `tabLockApply()` (setzt/entfernt `.tab-locked` auf den fünf Buttons; wechselt via `_switchTabInternal("setup")` auf Implantat-Reiter wenn aktiver Tab gesperrt wird; ruft am Ende `subtabLockApply()` und `playerLockApply()` für L2/L3-Nachzug), `tabLockShowModal(reason)` (füllt und öffnet `#tabLockModal` — seit BA 173 mit drittem Zweig `reason === "sideDeaf"`), `tabLockHideModal()` (schließt Modal). Eigener `DOMContentLoaded`-Handler für Close-Button. `switchTab` ruft nach Test-Guard den neuen Sperr-Guard auf und delegiert danach an `_switchTabInternal`. **BA 173 — Sub-Tab-Sperre L2 (eine Seite taub):** Konstante `LOCKED_SUBTABS_L2` (`{messungen: ["balance","latenz","freqmatch"]}` — Vergleichstests, die bei tauber Seite nicht sinnvoll sind). Funktionen: `evalDeafState()` (liefert `{hasDeaf, deafSide}`; deafSide ist `"left"` vor `"right"`), `subtabLockApply()` (setzt/entfernt `.tab-locked` auf den drei Sub-Tab-Buttons; wechselt bei aktivem gesperrtem Sub-Tab via `_switchSubtabInternal(parent, "test")` zurück auf Elektrodenlautstärke). `switchSubtab` ruft eingangs den L2-Sperr-Guard auf und zeigt `tabLockShowModal("sideDeaf")` bei Klick auf gesperrten Sub-Tab. Sperre umfaßt Top-Level-Tabs **und** Sub-Tabs in Messungen. Globale Variable `_suppressHashPush` (bool) unterdrückt `history.pushState`-Aufrufe während Init und popstate-Restore. `popstate`-Listener am Dateiende reaktiviert Tab/Subtab bei Browser-Navigation (Zurück/Vor). |
| 14 | levels.js | `calcPresetCurve`, `getTotalPresetCurve`, `getEffectiveLevels` (noch in expText/file.js genutzt), `buildPrTbl`, `drawLvChart`, `lvOnChange`, `applyPresetDeltaOtherSide`. `buildLvGrid`, `updLvFocus`, `updAllBars` sind entfernt. Seit BA 68: `calcPresetCurve` (tilt/scurve/pivot/gauss/speech) und `drawLvChart` (x-Achse) verwenden `effFreqDisplay` statt `effFreq` — bei aktivem Frequenz-Warping folgen Kurvenberechnung und Chart den gewarpten Wahrnehmungs-Frequenzen. Bass-/High-Boost und Volume bleiben elektrodennummern-basiert (kein Warp-Einfluß). |
| 14a | rubberband-loader.js | Lazy-Loader für die rubberband-wasm-Library (`vendors/rubberband-wasm/dist/`). Exportiert `rubberbandLoad()` (Promise<RubberBandInterface>), `rubberbandIsLoaded()` (bool) und `rubberbandLastError` (string\|null). Fetcht `vendors/rubberband-wasm/dist/rubberband.wasm` beim ersten Aufruf, kompiliert das WASM-Modul und liefert eine initialisierte `RubberBandInterface`-Instanz zurück (Singleton). Folge-Aufrufe geben dieselbe Instanz aus dem Cache zurück. Fehler-Handling für file://-Block und WASM-Compile-Fehler mit sprechenden Meldungen (werden in BA 186 dem Nutzer angezeigt). Geladen nach `vendors/rubberband-wasm/dist/index.umd.min.js` (UMD-Bundle, stellt globales `rubberband`-Objekt bereit). (BA 184) |
| 14c | smplr-loader.js | Lazy-Loader für die smplr-Library (`vendors/smplr/dist/smplr.esm.js`, ESM-Bundle). Exportiert `smplrLoad()` (Promise<Modul>), `smplrIsLoaded()` (bool), `loadMellotron(ctx, variant)`, `loadSamplerByToken(ctx, token)`, `smplrSamplerIsReady(token)`, `smplrLastError`. Singleton-Cache pro Token (`smplr:mellotron:<variant>`). Bezieht das ESM beim ersten Aufruf per dynamic `import()`. Wird ab BA 225 in `playToneTyped` aufgerufen. **Seit 3.2.226.5-beta**: Soundfont2 entfernt — `SMPLR_SF2_KEYS`, `loadSoundfont2`, `vendors/soundfont2/` und das UMD-Vorladen in `index.html` entfallen; Klang war stumm (race condition) und Mellotrons 34 Varianten decken die Klangpalette ab. (BA 224) |
| 14b | levels-tab.js | Schieber-Tab (sichtbar „Levels"): `lvTabDraw` (Dispatcher), `_lvTabBuildAxis` (lokale Hilfsfunktion: gleichmäßige x-Verteilung der Elektroden über die Plot-Breite, rein elektrodennummern-basiert — kein Frequenzbezug), `lvTabDrawRelative`, `lvTabDrawAbsolute` (beide elektrodennummern-basiert via `_lvTabBuildAxis`; Säulen sitzen an gleichmäßiger x-Position, Säulenbreite an `axis.minDx` gekoppelt; pro Spalte wird `xMid` in das `col`-Objekt geschrieben), diverse Helper-Zeichenfunktionen (`lvTabDrawExcludedColumn`, `lvTabDrawNoMclColumn`, `lvTabDrawStackRelative`, `lvTabDrawSumBarRelative`, `lvTabDrawStackAbsolute`, `lvTabDrawSumBarAbsolute`, `lvTabDrawFocusAndSum`, `lvTabDrawLabelsRelative` (einzeilig: nur Elektroden-Bezeichnung), `lvTabDrawCompareLinesRelative`, `lvTabDrawCompareLinesAbsolute` (beide nehmen `col.xMid` statt `slotW` zur Positionierung)), `lvTabAbsoluteAvailable`, `lvTabUpdateModeAvailability`, `lvTabElHasMcl`, `lvTabNavigableEl`, `lvTabStepAbsolute`, `lvTabUpdateWarpHint` (steuert Sichtbarkeit des dezenten Warp-Hinweises unter dem Canvas; sichtbar nur bei `pWarpOn && lvTabShowCurves`), `lvTabRebuild`, `lvTabOnSchieberChange`, `lvTabResetAll`. Kein x-Achsen-Tooltip im Schieber-Tab — der Schieber verändert ausschließlich dB-Korrekturen pro Elektrode, daher kein Frequenzbezug auf der x-Achse. Eigener DOMContentLoaded-Handler mit focus/blur-Listenern auf dem Canvas. State: `lvTabFocus` (lokal), `lvTabHasFocus` (lokal, true wenn Canvas Tastatur-Fokus hat — steuert die Umrahmung der aktiven Elektrode), `lvTabShowMeas`, `lvTabShowCurves`, `lvTabMode`, `lvTabVariant` (alle in state-side.js). Canvas hat `tabindex="0"`, damit es per Klick oder Tab-Taste fokussierbar ist. |
| 15 | player.js | Player-State, `gPC`, `pBuildEQ`, `pUpdEQ`, `pPlay` (async), `pDrawEQ` + eigene Top-Level-Listener für plAudio/plPlay/plStop/plTL und window.resize. **BA 192 — Vereinheitlichte Wiedergabe-Card:** Zentrale Steuer-Funktionen am Dateiende: `plPlayPauseToggle`, `plStopAll`, `plPrev`, `plNext`, `plToggleLoop`, `plToggleShuffle` (BA 258), `plToggleAutoAdvance`, `plSetPause`, `plSetSource`; UI-Sync: `plUpdSourceUI`, `plUpdTransportUI`, `plUpdDisplay`, `plRefreshTooltips`; Idle-Timer: `_plArmIdleTimer`, `_plClearIdleTimer`, `_plNoteInteraction`, `_plAutoAdvCancel` (Auto-Advance stoppt nach 30 min ohne Interaktion). `plPlay`/`plStop`-Listener umgehängt auf `plPlayPauseToggle`/`plStopAll`. `plCtrl`-Container entfernt (Transport-Leiste immer sichtbar). Neue Top-Level-Listener: `plSrcMusicBtn`, `plSrcSentencesBtn`, `plSrcNoiseBtn`, `plPrev`, `plNext`, `plLoopBtn`, `plAutoAdvBtn`, `.pl-pause-btn`, `.pl-vol-btn`, `plSentShowText`. **BA 193 — Geräusche-Quelle + Fixes:** Geräusch-Logik: `plNoiseRefreshUI` (befüllt Sort-, Kategorie- und Item-Dropdown; ruft am Ende `plSentBgRefreshUI` auf), `plNoiseCurrentItem`, `plNoiseLoadSelected` (async; nutzt seit BA 194 `amGetItemBuffer` aus audio-source.js). **BA 262 — Vier-Elemente-UI für Geräusche:** `_plNoiseSearchMatch`, `plNoiseAllItems`, `plNoiseVisibleItems` (kombiniert Kategorie-Filter via `amItemMatchesCategory` und Suche); `plNoiseRefreshUI` auf gefilterte Sicht umgestellt; `_plNoiseStep` und Auto-Advance-Pfad nutzen `plNoiseVisibleItems`. Lautstärke-Schnellbuttons: `plUpdVolBtns` (hebt aktiven Wert hervor). `pSetPlaybackMode` kennt jetzt drei Modi: `"file"`, `"sentence"`, `"noise"` (setzt `pSourceBuf` aus `pNoiseBuf`). Bis 3.2.209.1 setzte `pPlay` `pSrc.loop = true` für Loop; seit 3.2.209.2 wird Loop einheitlich über den `onended`-Handler mit `plPauseMs`-Pause realisiert — damit greift die eingestellte Pause auch bei Loop, und Loop-Aus während laufender Wiedergabe wirkt sofort (aktuelles Stück läuft fertig, kein Restart). `onended`-Handler ergänzt um Noise-Auto-Advance-Zweig. Pause-Buttons sind jetzt immer aktiv (unabhängig von `plAutoAdvance`). **BA 194 — Hintergrund-Geräusch für Sätze:** UI-Logik: `plSentBgRefreshUI` (befüllt Dropdown aus `amCollectItems`, setzt Toggle-Farbe und SNR-Button-Hervorhebung), `plSentBgToggle`, `plSentBgSetItem`, `plSentBgSetSnr` (alle leeren `amMixCacheClear` bei Änderung). Listener für `#plSentBgToggleBtn`, `#plSentBgSel`, `.pl-snr-btn` am Dateiende. **BA 195 — Hörbuch-Quelle (lokal):** `AM_AUDIO_EXT` (Regex für Audiodatei-Endungen), `plBookHandleUpload(fileList)` (async; sortiert Dateien, erzeugt Collection mit Blob-URLs), `plBookCurrentCollection()`, `plBookCurrentChapter()`, `plBookRefreshUI()` (befüllt Sort/Buch/Kapitel-Dropdowns), `plBookLoadSelected()` (async; dekodiert Kapitel via fetch + decodeAudioData, ruft `pSetPlaybackMode("book")`, stellt Position aus `plBookPositions` wieder her), `plBookSavePosition()` (speichert chapterIdx + posSeconds in `plBookPositions`). `pSetPlaybackMode` kennt jetzt auch `"book"` (setzt `pSourceBuf` aus `pBookBuf`). `onended`-Handler um Hörbuch-Auto-Advance-Zweig erweitert (stoppt still am Buch-Ende). `plSetSource("audiobook")` jetzt nicht mehr blockiert; speichert bei Verlassen die Buchposition. `plUpdSourceUI` hebt Audiobook-Button korrekt hervor. `plUpdDisplay` ergänzt um Hörbuch-Zweig. `plPlayPauseToggle` lädt Kapitel bei fehlendem Buffer. `plStopAll` speichert Buchposition vor Stop. **BA 258 — Zufall-Toggle + Prev-Memory:** `plToggleShuffle`, `plNoiseHasPrevMemory`, `plBookHasPrevMemory`; `_plNoisePrevId` (1×-Memory Geräusch), `_plBookPrevChIdx` (1×-Memory Hörbuch-Kapitel). `_plNoiseStep` und Auto-Advance-Pfade für Noise/Audiobook auf Zufall/Sequenz umgestellt; `plPrev`/`plNext`-Audiobook-Zweige mit Shuffle-Logik. `plUpdTransportUI` trennt `hasNext`/`hasPrev` (Prev im Zufall-Modus per Memory-Check deaktivierbar). `plPrev`/`plNext` springen bei Hörbüchern kapitelweise. `hasNext` in `plUpdTransportUI` schließt `"audiobook"` ein. Listener am Dateiende: `plSrcAudiobookBtn`, `plBookUploadBtn`, `plBookUploadInput`, `plBookSortSel`, `plBookSel`, `plBookChSel`, `plBookRemoveBtn`. **BA 260 — Musik-Bibliothek (Sub-Block):** Musik-Sub-Block mit Suchfeld, Sortier-Achse, Kategorie-Dropdown, Stück-Dropdown. amProvider `"music-local-file"` liefert das hochgeladene Stück. Filter-/Sortier-Logik: `plMusicRefreshUI`, `plMusicVisibleItems`, `plMusicCurrentItem`, `plMusicLoadSelected`, `plMusicSetSelected`, `_plMusicStep`, `plMusicHasPrevMemory`; `_plMusicPrevId` (1×-Memory). Prev/Next, Shuffle, Auto-Advance analog Geräuschen; `onended`-Handler um Musik-Auto-Advance-Zweig erweitert (kein Wrap-Around am Listenende). **BA 261 — Ordner-Upload + Webspace:** `plMusicRefreshLocalList()` (rendert Liste geladener Ordner mit Anzahl und Entfernen-Button); `plMusicLoadSelected` um `local-music-folder:`-Zweig erweitert (löst Ref via `amMusicResolveLocalFile` auf). IIFE `_plMusicWire` verdrahtet zusätzlich `#plMusicLocalAddBtn`/`#plMusicLocalInput` für Ordner-Ingest. **BA 173 — Player-Bereich-Sperre L3 (eine Seite taub):** Funktion `playerLockApply()` am Dateiende; disabled `plBalApplyBtn`/`plBalModeSelect`/`plLatApplyBtn`/`plWarpOn` (Opazität 0.4) und blendet Inline-Hinweis-Spans `plLockHintBal`/`plLockHintLat`/`plLockHintWarp` mit i18n-Key `plLockHintSideDeaf` ein, wenn `evalDeafState().hasDeaf` true ist. Aufruf erfolgt automatisch aus `tabLockApply()` in tabs-eq.js — keine eigenen Hooks nötig. State: `pSrc` (Einzel-Quelle), `pCurrentPlayback` ({sources, stop()} für Variante B/A), `pPlayGen` (Generationszähler gegen Stale-Async), `pMaplawOn`, `pMaplawSollC`, `pMaplawNode`, `pFileBuf` (Audiodatei-Buffer, überlebt Sätze-Wiedergabe), `pPlaybackMode` (`"file"`|`"sentence"`|`"noise"`|`"book"`), `pWarpComputingPromise` (Handle auf laufende Warp-Berechnung; in `pSetPlaybackMode` gesetzt, in `pPlay` awaited). Buffer-Steuerung: `pSetPlaybackMode(mode)` setzt `pSourceBuf` auf den aktiven Slot, baut EQ neu und löst bei aktivem `pWarpOn` automatisch `pWarpTrigger()` aus (seit 3.2.209.1 zentralisiert — vorher mussten alle Aufrufer den Trigger selbst nachziehen, was im Sätze-Pfad fehlte und Warping dort wirkungslos ließ). Seit 3.2.209.2 wird der laufende Compute-Promise zusätzlich in `pWarpComputingPromise` festgehalten, damit `pPlay` darauf warten kann. Seit 3.2.209.3 aktualisiert `pSetPlaybackMode` zusätzlich `plTot`/`plCur`/`plTL` (Gesamtzeit-Anzeige + Slider-Reset) — vorher fehlte die Aktualisierung beim Sätze-Wechsel. Aufrufer dürfen Position danach überschreiben (Hörbuch-Position-Restore). MAPLAW-Helfer: `pMaplawGetIstC`, `pMaplawIsApplicable`, `pMaplawTrigger`, `pMaplawUpdUI` (UI-Sync: Ist-c-Anzeige, Soll-c-Display neben dem Ist-Wert, Toggle-Zustand, Soll-c-Eingabe, Nicht-MED-EL-Hinweis). `pApplyShowExperimental` synchronisiert die Sichtbarkeit der MAPLAW-Card (`plMaplawCard`) sowie des Hinweistexts (`plExperimentalHint`) anhand von `plShowExperimental`. Frequenz-Warping ist nicht mehr experimentell (eigene Einstellungsbox `plWarpSettingsBox`, gesteuert durch `pWarpUpdUI`). Seit BA 69: `pBuildEQ`, `pCompQ` und `pDrawEQ` verwenden `effFreqDisplay` statt `effFreq` — bei aktivem Frequenz-Warping folgen EQ-Filter-Frequenzen und x-Achse des EQ-Graphen den gewarpten Wahrnehmungs-Frequenzen. Im „both"-Modus setzt `pBuildEQ` pro Channel `effFreqDisplay(i, "left"/"right")`; im Mono-Modus bindet `effFreqDisplay(i)` ohne Side-Argument an `activeSide`. |
| 16 | freq-warp.js | Frequenz-Warping (Rubberband). `buildWarpPoints`, `_warpAffectedSides`, `centShift`, `pComputeRubberbandWarpedBuffer` (vollständig seit BA 186; Helper: `_rbDecideAffectedSides`, `_rbBuildBandEdges`, `_rbDesignBandpassFIR`, `_rbConvolveViaWebAudio`, `_rbPitchShift`, `_rbProcessMonoSide`, `_rbBuildOptionBits`; Konstanten: `_RB_FIR_ORDER`; Lazy WASM-Load via `rubberbandLoad()`), `pWarpTrigger`, `pWarpUpdUI`. Zentrale Quelle für Warp-Stützpunkte: `_warpFResSource()` (seit BA 103 dreistufig: fRes final > in-progress aus `_fmrBuildInProgressEntries` > Slider-Schätzungen aus `_fmrBuildSliderEntries`, jeweils Vorrang pro (varSide, elIdx)); `_warpFResStats()` liefert seit BA 103 `{total, finals, provisional, sliderEst}` für UI-Anzeige. Player-Status-Banner hängt bei `sliderEst > 0` den String `pwStatusSliderEst` an. **Seit BA 189:** `pWarpCancelCompute()` setzt `pWarpCancel=true` und bricht `_rbProcessMonoSide` zwischen Bändern ab (wirft `__warp_cancelled__`); `pWarpTrigger` fängt den Fehler, schaltet `pWarpOn=false` und Checkbox aus. `pWarpUpdUI` steuert zusätzlich Sanduhr (`#plPlayBusyIcon` neben Play-Button), Stop-Button (`#plWarpStopBtn`, nur Rubberband), Tooltip-Text (`#plPlayBusyTip`), Prozentanzeige im Status-Text (via `pWarpProgress`), Fortschrittsbalken im Transport-Bereich (`#plWarpProgressRow`, `#plWarpProgressBar`, `#plWarpProgressPct`; sichtbar nur wenn `pWarpBusy && pWarpProgress > 0`). DOMContentLoaded-Handler am Dateiende initialisiert Tooltip-Sichtbarkeit (mouseenter/mouseleave/click am `#plPlayWrap`, click-außen und Escape am Document). State: `pWarpedBuf`, `pWarpOn`, `pWarpSettingsOpen` (ob die Settings-Box aufgeklappt ist, Default `false`, gesteuert via `plWarpSettingsToggle`-Button unabhängig von `pWarpOn`), `pWarpMode`, `pWarpStrength`, `pWarpBusy`, `pWarpCancel` (Stop-Flag für Rubberband-Abbruch), `pWarpProgress` (0..1), `pWarpAffected`, `pRubberbandOptions` (Engine, Material, Formant, Fast; BA 191), `pWarpGen` (seit 3.2.209.4 — Generation-Zähler: neuer `pWarpTrigger`-Aufruf inkrementiert ihn, bricht laufenden Vorgänger via `pWarpCancel=true` ab und wartet auf dessen Beendigung; der überholte Run erkennt `myGen !== pWarpGen` und räumt still auf ohne `pWarpOn` auszuschalten — User-Cancel via Stop-Button oder Warp-Toggle-Klick während laufender Berechnung sind die einzigen Pfade, die `pWarpOn=false` auslösen; ab BA 213 ruft auch der Toggle-Click-Handler in `init.js` `pWarpCancelCompute()` auf, wenn `pWarpBusy` gesetzt ist, und `pWarpTrigger` spielt nach User-Cancel ungewarpt weiter). **Seit BA 178** (`pWarpMode`-Wertebereich von `ref_side`/`var_side`/`symmetric` umgestellt auf absolute Seiten `left`/`right`/`symmetric`; HTML-Optionen mit i18n-Keys `pwModeLeft`/`pwModeRight`/`pwModeSym`): `_migrateLegacyWarpMode(savedMode, savedFRes)` übersetzt Alt-Werte aus Save-Dateien anhand der refSide des ersten fRes-Eintrags in absolute Seiten (Fallback `right`); `pApplyWarpModeDefaultFromFm()` setzt einmalig pro Session beim Übergang `fRes/sliderEstimates 0→1+` den Modus auf die Gegenseite der Frequenzabgleich-Referenzseite (sym→symmetric); `pMarkPlayerWarpDefaultAsApplied()` setzt das Session-Flag `_pPlayerWarpDefaultApplied` direkt nach dem Save-Load, damit ein geladener Modus beim nächsten Mess-Insert nicht überschrieben wird. Aufrufer von `pApplyWarpModeDefaultFromFm()`: zwei Push-Stellen in `freqmatch-adaptive.js` (Aggregat-Insert und finaler Insert) und zwei Schreibstellen in `freqmatch-slider.js` (`fmConfirm` und `fmRunSliderDebugSim`).  |
| 16b | maplaw.js | MAPLAW-Simulation Phase 3 (bandweise Hüllkurven-Vorverzerrung Ist⁻¹∘Soll für MED-EL). `_MAPLAW_PROCESSOR_CODE` als Worklet-Inline-String mit Filterbank (12 Biquad-Bandpässe an MED-EL-Frequenzen, Q=4), Hüllkurven-Detektor (Gleichrichtung + IIR-Tiefpaß 50 Hz), lokale Normalisierung (gleitendes Maximum, τ=1 Sek), MAPLAW-Kennlinie + Inverse, **additive Korrektur** (out = x + Σ y_b·(gain_b−1), nicht out = Σ y_b·gain_b — sonst klingt schon der Identity-Fall verfärbt, weil die naive Bandpass-Summe keine perfekte Rekonstruktion ist). `pInitMaplawWorklet`, `pBuildMaplawNode`, `pMaplawApplyParams`. Bei `active=0` oder `istC == sollC`: Passthrough. **Stereo-fähig**: Worklet hält pro Kanal (L/R, `MAX_CH=2`) eigenen Filterbank-, Hüllkurven- und Max-State und verarbeitet jeden Kanal separat über `_processChannel`. `pBuildMaplawNode` konfiguriert den Node mit `channelCount: 2`, `channelCountMode: 'explicit'`, `outputChannelCount: [2]` — dadurch wird Mono-Input vor dem Worklet auf L=R aufgeteilt (sonst sähe der Worklet bei `mode='right'` nur den stillen linken Kanal von `pRightOnlyBuf`) und Stereo-Input ohne Downmix durchgereicht. Worklet wird in Bauanleitung 19 in den Player-Audio-Graph eingehängt; UI kommt in Bauanleitung 20. |
| 17 | lr-balance.js | Stereo-Balance-Tab. Nutzt `buildTestPanel` (testUI-API, BA 245). Eigener DOMContentLoaded-Handler und eigener Tab-Hook für `balance`. Globale Variable `lrSnapshot` (BA 156): Schnappschuß der Implantat-Felder zum Zeitpunkt der ersten LR-Messung (`null` = noch keine Messung oder Daten gelöscht). Wird beim ersten `lrConfirm`-Aufruf gesetzt, beim Löschen und Reset genullt, in `saveJson`/`loadJson` mitgespeichert. `lrSelectedEls` (BA 245): Elektroden-Auswahl für den Test (`null` = alle testbaren). Die alte Slider-Range-Logik (`LR_SLIDER_RANGES`, `_lrRstSlR`, `_lrExtSlider`) ist entfernt; der Slider nutzt Auto-Extend über `testUI.slider.setValue`. Die alte `lrSetSideActive`-CSS-Funktion ist entfernt; Aufleuchten läuft über `testUI.pairIndicator.setPlaying`. Stop-Semantik: `lrPause()` (Sequenz bleibt erhalten, Resume-fähig); `lrFinish()` setzt Sequenz zurück. **BA 253 — Tonart/Vol/Dur/Pau ins Tonauswahl-Modal:** `lrGVol`/`lrGDur`/`lrGPau` lesen aus `volume_balance`/`duration_balance`/`pause_balance`. `lrPlayTone` und `lrPlaySimul` nutzen `toneType_balance`. `tonePopupButton`-Konfig im `common`-Bereich mit `keyboardMode: true`, `getDisabledElectrodes`. Neue Helfer-Funktionen: `_lrTpKbdN` (Tasten-Anzahl = min(leftN, rightN)), `_lrTpElectrodeFreqs`, `_lrTpElectrodeLabels`, `_lrTpDisabledElectrodes` (Taste disabled wenn auf einer Seite abgewählt oder ausgeschlossen). Modul-Scope-State: `_lrTpCorrectVol` (Callback aus `onTogglesReady`), `_lrTpModalTone` (aktuell im Modal gewählte Tonart). **BA 255 — Seitenabfrage:** `lrHookOnStart` enthält nur noch den `testUI.sideCheck.run`-Wrapper; der eigentliche Start-Code liegt in der neuen privaten Funktion `_lrDoStart`. |
| 17c | latency.js | Latenz-Messung (Inter-Ohr-Zeitversatz). State: `latencyResult` ({valueMs, clickType, intervalMs, timestamp, implantSnapshot: Snapshot zum Zeitpunkt der Messung — BA 156}), `plApplyLatency`, `latSliderMs`, `latActive`, `latClickType`, `latIntervalMs`. Audio-Nodes: `pLatSplitter`, `pLatDelayL`, `pLatDelayR`, `pLatMerger` — werden von `latInitGraph` einmalig zwischen `pGain` und `c.destination` eingehängt. Funktionen: `latBuildClickBuffer`, `latBuildBurstBuffer`, `latBuildLoopedTestBuffer`, `latStartTest`, `latStopTest`, `latSetSliderMs`, `latApplyToPlayer` (ruft `updLatApplyBtn` zur Button-Synchronisation), `latInitGraph`. UI-Funktionen: `latApplyAsResult` (wird intern von `latStopTestUI` gerufen — kein Button), `latRenderResults` (Ergebnis-Sub-Tab), `latUpdateValueText`, `latUpdateIntervalHint`, `latUpdateButtonStates` (disabled-Steuerung Schieber + Buttons). ENTER-Listener im DOMContentLoaded-Handler: stoppt laufenden Test via `latStopTestUI`, greift nur wenn `latActive === true`. Eigener DOMContentLoaded-Handler. |
| 17b | sentences.js | Sätze-Wiedergabe im Player. Hybrid-Loader: erst `fetch` auf `sentences.json` (online-Voll-Korpus); bei Fehlschlag Wechsel in `sOfflineMode` und Lazy-Load der Embed-Modul-Datei `assets/sentences/embed/<lang>.js` per dynamischem `<script>`-Tag (file://-kompatibel). Audio aus Embed-data:-URLs wird über `sDataUrlToArrayBuffer` direkt in den AudioContext geleitet. Schema: `speakers.<key> = {lang, label, kind, source, license, credit, recordings:[{id,text,audio,...}]}`. **BA 197 — Sätze auf amProvider-System umgestellt:** `sCurRec` ist jetzt ein flaches Item im amProvider-Schema (kein `{speakerKey, recIdx, rec}` mehr). `sBuildRecordingPool(spkSel)` und `sRefreshSpeakerDropdown` aggregieren über `amCollectItems("saetze")`. Zwei neue Provider am Dateiende (außerhalb DOMContentLoaded): `"sentences-legacy"` (exponiert `sCorpus` als Items) und `"sentences-local"` (exponiert `sLocalCollections`). `sPlayCurrent` handhabt zusätzlich `https?:`/`blob:`-URLs für Webspace-Sätze. State: `sActive`, `sEndless` (true = Endlosfolge-Modus), `sEndlessCount` (Zähler abgespielter Sätze in laufender Endlosfolge, Stop bei 100), `sCurRec` (flaches amProvider-Item), `sCorpus`, `sLoaded`, `sLoading`, `sShownText`, `sSentenceBuf` (dekodierter aktueller Satz, getrennt von `pFileBuf`), `sPauseTimer`, `sPauseMsVal`, `sOfflineMode` (true = fetch hat versagt, Embed-Modus aktiv), `sEmbedLoading` (Set laufender Sprach-Ladevorgänge), `sLocalCollections` (Map collectionId→Collection-Objekt für lokale Ordner; jedes Objekt hat zusätzlich `handleId` (String oder null, IndexedDB-Schlüssel für den FSAA-Handle), `persistable` (true wenn über FSAA geladen), `stub` (true wenn nach JSON-Restore noch nicht gemountet)), `sLocalNextId` (Zähler für eindeutige Collection-IDs). `sEnsureEmbedForLang(langCode)` lädt on-demand `embed/<lang>.js` und mergt Sprecher in `sCorpus`. `sBuildRecordingPool(spkSel)` liefert das Pool-Array gemäß UI-Auswahl ("any" = alle Sprecher der aktuellen Sprache flach gemischt). Bedienung über drei Buttons: `sPlay` (aktueller Satz einmal), `sNext` (anderer zufälliger Satz, einmal), `sEndlessStart` (Endlosfolge). Hilfsfunktionen `sPickRandom(pool, exclude)`, `sBuildSequencePool` (BA 258: alle Items nach Sprecher blockweise sortiert, DE zuerst, innerhalb Sprecher nach recording.id), `sSeqStep(delta)` (±1 in der Sequenz), `sHasPrevMemory` (prüft `sPrevRec`). **BA 258 — sequentiell + Prev-Memory:** `sNext` schaltet je nach `plShuffle` zufällig oder sequentiell; neue `sPrev`-Funktion (Zufall: Memory-Verbrauch; sequentiell: echter Schritt zurück); `sPrevRec` (1×-Memory für letztes Item). `sPlay` wählt im sequentiellen Modus das erste Item der Sequenz statt `sPickRandom`. `sOnEnded`-Auto-Advance-Pfad auf Zufall/Sequenz umgestellt. Wiedergabe setzt `pSourceBuf` via fetch+decodeAudioData (online) oder `sDataUrlToArrayBuffer` (offline/data:-URL) oder `file.arrayBuffer()` (lokal) und ruft `pPlay()`. Audio-Ref-Schema: `"<pfad>"` (fetch), `"data:…"` (Embed), `"local:<collectionId>:<relPath>"` (lokaler Ordner). `sOnEnded()` wird aus dem onended-Handler in `player.js` getriggert; im Endlosmodus nächste Recording wählen, sonst stoppen. Mutual Exclusion zur Musikdatei unverändert. Sprecher-Dropdown wird dynamisch von `sRefreshSpeakerDropdown()` befüllt — ruft `sSpeakersForLang(lang)`; installiert außerdem `sel.onchange` für Stub-Re-Mount (Klick auf „(nicht geladen)"-Eintrag öffnet Picker). `sUpdateUI()` wird von `applyLang()` gerufen (Sprachwechsel) und triggert im Offline-Modus `sEnsureEmbedForLang`. IndexedDB-Wrapper: `sIdbOpen`, `sIdbPut`, `sIdbGet`, `sIdbDel` (DB „ciSoundBalancing", Store „folderHandles"). `sFsaaAvailable()` prüft `window.showDirectoryPicker`. FSAA-Ingest: `sIngestFromHandle(rootHandle, handleId)` — rekursiver Walk per `dirHandle.entries()`, `Object.defineProperty` für `webkitRelativePath`, ruft `sIngestLocalFolder`, speichert Handle in IDB, setzt `handleId`/`persistable` an neuen Sammlungen. Picker-Hybrid im Click-Handler: FSAA-verfügbar → `showDirectoryPicker`, sonst `<input webkitdirectory>`. Restore: `sRestoreLocalCollections(metaArr)` — nach JSON-Load aufgerufen, holt Handle aus IDB, fragt Permission, ruft `sIngestFromHandle`; bei Fehlschlag Stub-Sprecher. `sReloadStubCollection(cid)` — Re-Mount bei Klick auf Stub im Dropdown (FSAA oder webkit-Fallback). Lokale Ordner-Funktionen: `sNewCollectionId`, `sIngestLocalFolder` (Ingest-Einstieg, ruft Heuristiken auf), `sDetectFreiburger`, `sDetectOldenburger`, `sLoadOldenburgerManifest`, `sParseOldenburgerManifest`, `sParseGenericManifest`, `sLoadGenericManifest`, `sBuildFreiburgerRecordings`, `sBuildOldenburgerRecordings`, `sBuildGenericRecordings`, `sMakeSpeaker`, `sRefreshLocalList`, `sRemoveLocalCollection` (jetzt async — löscht auch IDB-Handle wenn nicht mehr referenziert). Eigener DOMContentLoaded-Handler. Datenstruktur unter `assets/sentences/`: `thorsten/01.mp3…50.mp3`, `cv-de/`, `cv-en/`, `cv-fr/`, `cv-es/` je 100 MP3s + manifest.json. Offline-Embeds unter `assets/sentences/embed/<lang>.js`. |
| 18 | init.js | Der große DOMContentLoaded-Handler mit `applyLang()`, `buildImplantCard()`, allen Event-Verdrahtungen, Autosave-Setup. Seit BA 106: Autosave-Load nutzt die gleiche Migrations-Sequenz wie der JSON-Load (`_fmCleanupLegacyFRes` → `_fmMigrateAltSliderFRes`). Seit BA 161: Autosave-Block als eigene Funktion `_autoSaveState()` (innerhalb des DOMContentLoaded-Handlers) ausgelagert; per `window._autoSaveState = _autoSaveState` global verfügbar gemacht, damit `resetAll()` in file.js nach einem Reset sofort persistieren kann. Autosave speichert jetzt zusätzlich `audiologUserNote`, `lrSnapshot` und `fmMode`/`fmAdaptiveDur`/`fmAdaptivePau` pro Seite; Warp-Schlüssel: `warpOn`/`warpMode`/`warpStrength` (Lade-Routine akzeptiert alte `pWarp*`-Namen als Fallback; `warpMethod` wird beim Laden ignoriert). |
| 19 | legal.js | Footer-Modals: `_legalOpenImprint`, `_legalOpenLicense`. Impressum-Inhalt statisch in `_legalBuildImprintBody` (deutsch, gemäß § 5 DDG). E-Mail wird via `_legalAssembleEmail` erst beim Öffnen aus zwei Bestandteilen zusammengesetzt (Spam-Schutz). Lizenz-Modal (GNU GPL v2+, GPL-2.0-or-later) lädt per `fetch` von raw.githubusercontent.com mit Fallback-Link bei Netzwerk- oder CORS-Fehler. Eigener DOMContentLoaded-Handler verdrahtet die drei Footer-Links und Close-Buttons. Konstanten `_LICENSE_RAW_URL`, `_LICENSE_HTML_URL`. |
| 20 | finanzen.js | Datenhaltung + Berechnungen für den Unterstützung-Tab. Pflege-Block oben (alles, was Martin editieren muß): `FINANZEN_BEGIN` (YYYY-MM, linke Kante des Graphen), `FINANZEN_POSTEN` (full/current pro Monatsposten), `FINANZEN_DAUER` (ein Eintrag pro Dauerspende, mit `start`/`end`), `FINANZEN_EINMAL` (ein Eintrag pro Einmalspende, mit `date`). Berechnungen: `finBerechne()` (Ist-Werte für die Tabelle, API-kompatibel zur alten Fassung), `finBerechneZeitreihe(von, bis)` (monatsweise Reihe mit FIFO-Puffer aus Einmalspenden für den Graphen), Helper `finDauerAktivIn(monat)`, `finEinmalSummeBis(monat)`, `finFmtEuro(n)`, `finMonatHeute()`, `finMonatNext(m)`, `finMonatCmp(a,b)`. `finValidate()` läuft beim Laden und schreibt Warnungen für Format-/Typfehler in die Konsole. Keine DOM-Manipulation. Muß vor `unterstuetzung.js` geladen werden. |
| 21 | unterstuetzung.js | Rendert den Unterstützung-Tab. Befüllt `#untFinanzBody` / `#untFinanzFoot` / `#untGapHints` / `#untEinmalBlock` per `_untRenderFinanzTable()`. Der Einmalspenden-Block wird in `_untRenderEinmalBlock()` aus `finEinmalSummeBis(finMonatHeute())` gebaut und ausgeblendet, falls die Summe 0 ist. Bot-Schutz: IBAN (`_untBuildIban`) und E-Mail (`_untBuildMail`) werden erst beim Öffnen des jeweiligen Dialogs aus Fragmenten zusammengebaut. Dialog-Öffner `_untOpenDialog(id, builderFn)`. Eigener DOMContentLoaded-Handler verdrahtet `#untShowIbanBtn`, `#untShowMailBtn` und alle `[data-close-support]`-Buttons. Kein globaler State. |
| 21b | unterstuetzung-graph.js | Spenden-Verlauf-Graph (Canvas) für den Unterstützung-Tab. Pure Canvas-Zeichnung über `_ugRenderGraph()`: stacked Bars pro Monat aus `finBerechneZeitreihe(FINANZEN_BEGIN, heute+24)` (Dauerspenden grün, Puffer-Verzehr hellgrün, Lücke rot), horizontale Linien für `kostenCurrent`/`kostenFull`, vertikaler "Heute"-Marker. Helper `_ugMonatPlus`, `_ugMonatLabel`, `_ugT`. Eigene DOMContentLoaded-Verdrahtung, debounced Resize-Handler, optionaler `languagechange-applied`-Handler. Muß nach `unterstuetzung.js` (und nach `finanzen.js`) geladen werden. |
| 21a | update-check.js | Versions-Heartbeat mit Update-Banner (BA 183). Friert beim Laden die `APP_VERSION` ein und vergleicht sie bei jedem `visibilitychange → visible` sowie ~5 s nach `DOMContentLoaded` mit der Server-Version aus `js/version.js` (per `fetch(..., { cache: 'no-store' })`, regex-Match auf `APP_VERSION = "..."`). Bei Differenz und solange kein Test läuft (`body.test-running` fehlt) wird das Banner `#updateBanner` direkt vor `.tabs` eingefügt. Reload-Knopf macht `location.replace(location.pathname + '?t=' + Date.now())` für sicheren Cache-Bust; `×` blendet das Banner nur temporär aus (kein persistentes Dismiss — beim nächsten Fokus-Check kommt es wieder, solange die Differenz besteht). Re-Entrance-Guard `_checking`. Banner-Print über `@media print { #updateBanner { display: none !important; } }` unterdrückt. Eigener DOMContentLoaded-Handler. Test-Sperre wird in `test-ui.js` (`_startTest`/`_stopTest`) per `document.body.classList.add/remove('test-running')` gesetzt. |
| 22 | debug-tests.js | Selbsttest-Registrierung für das Debug-Panel (Bauanleitung 82). Lädt zuletzt, registriert via `dbg.test(...)` die vier Tests `global/player`, `global/i18n`, `global/sentence`, `frequenzabgleich/adaptiv`. Der Adaptiv-Test trägt `liveLogFor: "adaptiv.live"` — das Flag schaltet die `_fmDbg(...)`-Aufrufe in `freqmatch.js` an. Keine eigene UI, kein DOMContentLoaded-Handler. |
| 23 | debug-tests-current.js | Temporäre Bau-Diagnose-Tests aus laufenden Bauanleitungen (Bauanleitung 83). Lädt nach `debug-tests.js`. Tests heißen per Konvention `build/BAxx/<topic>` und tragen `opts.tab` der jeweiligen Bauanleitung. Pro Test ein eigener IIFE-Block mit Bauanleitungs-Nummer im Kopfkommentar. Nach Bau-Abnahme entscheidet Sonnet auf Nachfrage beim Nutzer: löschen oder verschieben nach `archive/debug-tests/BAxx_<topic>.js`. Beim Start (vor erster Nutzung) leer bis auf Header. |

## Datenfluss (nicht aus Namen ablesbar)

**Footer und Impressum:** Footer am Ende des `.container` (außerhalb
aller `.panel`-Container), enthält Versions-Anzeige, Impressum-Link,
Lizenz-Link (GNU GPL v2+) und GitHub-Link. Zwei `<dialog>`-Elemente
(`#imprintDialog`, `#licenseDialog`) werden über `legal.js`
verwaltet. Impressum-Inhalt deutsch und statisch; der Lizenztext
(GPL-2.0-or-later) wird beim Öffnen aus dem Repo nachgeladen.


**Touch-Bedienleisten:** Pro Slider eine `.touch-ctrl`-Box mit
Buttons − / Fein / + (`buildSliderTouchCtrl` aus touch-ctrl.js). Seit
BA 111 wird `buildSliderTouchCtrl` für den `slider`-Baustein der neuen
testUI-API automatisch durch `_buildTestPanelNew` aufgerufen (Optionen
`touchStep`/`touchFineStep` aus cfg; kein Replay-Button). Für die alten
Test-Module (test.js, lr-balance.js, latency.js) rufen diese weiterhin
`buildSliderTouchCtrl` selbst auf. Player-Stärke (`plStr`) und
Schieber-Tab (Canvas) haben Sonder-Implementierungen mit `attachLongPress`
direkt. Die Bedienleisten sind dauerhaft sichtbar (Desktop und Mobile).

**Kurven-Chart-Pinning:** Die `.lv-chart-card` im Kurven-Tab ist
nur auf Mobile-Breite (`max-width: 768px`) `position: sticky`.
Auf Desktop scrollt sie als normale Karte mit. Der Breakpoint
passt zu den anderen Mobile-Regeln in `style.css`.

**Mobile-Eingabe-Sperre:** Auf reinen Touch-Geräten (Smartphone)
werden alle Number-Inputs read-only, damit die System-Tastatur nicht
das Bild verdeckt. Eingabe läuft dort über die Touch-Buttons
(Bauanleitung 33). Erkennung über `IS_TOUCH_ONLY` aus `mobile.js`.
`applyMobileReadonly` wird nach jedem Rebuild dynamischer Tabellen
(`buildFreqTable`, `buildPrTbl`, `buildTestPanel`) erneut aufgerufen,
sonst greift das Flag nur auf den statischen HTML-Bestand.
`safeFocus` ersetzt direkte `.focus()`-Aufrufe an Stellen, wo der
Autofokus auf Touch-Geräten stören würde.

**Globaler State** liegt komplett in `state-side.js`. Wer auf
`sideData`, `activeSide`, `mfr`, `nEl`, `freqs`, `manualLevels`,
`presets`, `bRes`, `lvFocus`, Audio-State-Variablen, Test-State-
Variablen, die Tonfolge-Variablen pro Test (`sequence_freqmatch`,
`sequence_test`, `sequence_balance`), `slTarget_test`, `slTarget_balance` oder die
Levels-Tab-Anzeigestate (`lvTabShowMeas`, `lvTabShowCurves`,
`lvTabMode`, `lvTabVariant`) zugreift, findet die Deklaration dort.

**State pro Seite vs. globale Live-View:** Die persistierten
Daten leben pro Seite in `sideData.left.*` / `sideData.right.*`
(insbesondere `manufacturer`, `nEl`, `freqs`, `elFreqOwn`, `elSt`,
`elNt`, `elExDur`, `manualLevels`, `refEl`, `bRes`,
`presets`, `config`, `fullSweepRound`, `fullSweepDonePairs`,
`implant`). Die gleichnamigen freistehenden Variablen (`mfr`, `nEl`,
`freqs`, `manualLevels`, …) sind **Live-Views der aktiven Seite**:
`bindActiveSide()` kopiert die Referenzen aus `sideData[activeSide]`
in die globalen Variablen. Beim Side-Wechsel wird neu gebunden;
Tab-Module greifen i.d.R. auf die globalen Variablen zu und sehen
dadurch transparent die aktive Seite. Persistenz (JSON, localStorage)
serialisiert immer `sideData.left` und `sideData.right` getrennt.

**Bilaterale Konfiguration und Frequenzraster-Sync:** Jede Seite
hat eine `config` (`"ci"` / `"hg"` / `"normal"` / `"schwerhörig"`
/ `"taub"`). `setSideConfig(side, cfg)` setzt sie und ruft bei
Nicht-CI-Konfig `syncFreqsToAcoustic()`. `isSideUsable(side)` (BA 155)
liefert `true` wenn Hörtechnik gesetzt und — bei CI — Hersteller gesetzt;
wird als Cross-Modul-Helper von `freqmatch.js`, `test.js` und
`lr-balance.js` zur Voraussetzungs-Prüfung genutzt. `getFreqSource()` liefert
die Seite, deren CI-Frequenzraster auf eine akustische Seite
gespiegelt wird: wenn nur eine Seite CI ist, ist sie die Quelle;
sind beide CI (unabhängig) oder beide nicht-CI, liefert `null`.
Bei beiden nicht-CI greift `defaultMfr` als Frequenzraster-
Vorgabe (persistiert, `defaultMfrSelect` im Implantat-Tab). Beim
Spiegeln werden auf der Ziel-Seite `nEl`, `freqs`, `manufacturer`,
`elFreqOwn` und die pro-Elektroden-Arrays sowie `implant` auf
korrekte Länge gebracht — bestehende Werte bleiben erhalten, soweit
die Länge stimmt.

**Side-Wechsel:** Sichtbare Buttons `sideLeftBtn` / `sideRightBtn`
oben im UI rufen `setActiveSide(side)`. `setActiveSide` setzt
`activeSide`, ruft `bindActiveSide`, spiegelt den Wert in den
versteckten `ciSideSelect` (existiert aus historischen Gründen,
wird heute nur als interner Mirror benutzt), aktualisiert die
Dropdowns `mfrSelect`, `cfgSelect`, `defaultMfrSelect`, baut
Frequenztabelle, Preset-Tabelle, Kurven-Chart, Schieber-Canvas,
Ergebnisse und Implantat-Card neu und ruft `updSideButtons`
(visuelle Markierung des aktiven Buttons), `updFClearBtn`
(beschriftet den „Messergebnisse löschen"-Button mit der
aktiven Seite), `updPlSrcButtons` und ggf.
`updatePlayerForSideChange` (wenn ein Buffer geladen ist).

**Player Side-Modi:** `getPlayerSide()` (state-side.js) liefert
`"left"`, `"right"` oder `"both"` abhängig von Checkbox `plBothSides`.
„both" = Stereo mit getrennten EQ-Ketten pro Kanal (`pEqFLeft` /
`pEqFRight`), gespeist über `pChannelSplitter` und `pChannelMerger`.
„left"/"right" = nur die jeweilige Seite hörbar mit deren EQ-Kette;
der Gegenkanal ist stumm. `updatePlayerForSideChange` (player.js)
baut den Audio-Graph bei Side-Wechsel neu auf — auch während laufender
Wiedergabe (mit kurzer Unterbrechung).
EQ-Graph (`pDrawEQ`) und Werte-Tabelle (`pBuildTbl`) zeigen bei
`getPlayerSide() === "both"` die **aktive Seite** (`activeSide`), nicht
fest „left" — damit bei einseitigem CI die CI-Seite immer sichtbar ist.
Audio bleibt stereo. Bei `getPlayerSide() === "left"/"right"` (mono)
liefert `getPlayerGains()` ein flaches Array, kein `{left,right}`-Objekt
— der `gains.left !== "undefined"`-Guard trifft diesen Fall nicht.
Bei aktivem Frequenz-Warping verwenden `pBuildEQ`, `pCompQ` und `pDrawEQ` `effFreqDisplay` statt `effFreq` — EQ-Filter-Frequenzen und x-Achse des EQ-Graphen folgen den gewarpten Wahrnehmungs-Frequenzen. Im „both"-Modus wird pro Channel `effFreqDisplay(i, "left"/"right")` gesetzt, im Mono-Modus ohne Side-Argument (bindet an `activeSide`).

**Balance-Anwendungs-Modus** (`plBalanceMode`, state-side.js):
"sym" (Default), "left" oder "right". Steuert, wie der Balance-Wert
auf die beiden Channel-Gains verteilt wird (siehe
`getPlayerBalanceGains`). UI sichtbar nur bei `getPlayerSide() ===
"both"` und aktivierter Balance. Persistiert in JSON und
localStorage.

**MAPLAW-Simulation (Phase 3, MED-EL):** Bandweise Hüllkurven-Vorverzerrung Ist⁻¹∘Soll im AudioWorklet aus `maplaw.js`. Wird in `pPlay` zwischen letztem EQ-Knoten und `pGain` eingehängt, wenn `pMaplawOn` und `plEqOn` (EQ-Toggle als Master-Bypass) und `pMaplawIsApplicable()` (mindestens eine Seite MED-EL). Live-Updates von `pMaplawSollC` während Wiedergabe via `pMaplawTrigger` (postMessage an Worklet). Ist-c kommt aus `sideData[activeSide].implant.cValue`. Bilaterale Trennung mit zwei Worklets ist nicht implementiert — der unilaterale Standardfall ist abgedeckt. Die Soll-c-Anzeige im Settings-Block wird neben dem Ist-Wert live aktualisiert; die Listener in init.js für Quick-Buttons und Zahleneingabe rufen `pMaplawUpdUI` zusätzlich zu `pMaplawTrigger` auf.

**Frequenz-Warping — Persistenz:** Der Warp-Zustand (`pWarpOn`, `pWarpMode`, `pWarpStrength`, `pRubberbandOptions`) wird analog zu MAPLAW in beiden Persistenz-Pfaden vollständig gespeichert und wiederhergestellt: localStorage-Autosave (init.js, `_autoSaveState`, Schlüssel `warpOn`/`warpMode`/`warpStrength`/`warpRbOptions`) und JSON-Save/Load (file.js, Schlüssel `warpOn`/`warpMode`/`warpStrength`/`warpRbOptions`). Ältere Saves ohne `warpRbOptions` werden ohne Fehler geladen (Optionen behalten ihren aktuellen Wert). Ältere Saves mit `warpMethod`-Feld werden ohne Fehler geladen (Feld wird ignoriert). `pWarpedBuf` wird nicht gespeichert und bei Bedarf neu berechnet. Der JSON-Load-Pfad in `file.js` enthält kein Force-Off mehr (das frühere bewusste `pWarpOn = false` und `warpCb.checked = false` wurde in Bauanleitung 47 entfernt). UI-Sync nach dem Setzen über `pWarpUpdUI()`.

**Lokale Sammlungen — Persistenz:** Sammlungen, die über FSAA
(`showDirectoryPicker`) angelegt wurden, haben einen `handleId`,
unter dem der FileSystemDirectoryHandle in IndexedDB liegt
(DB "ciSoundBalancing", Store "folderHandles"). Beim JSON-Save
wandert die Metadaten-Liste mit (file.js `saveJson.localCollections`).
Nach JSON-Load ruft `applyLoadedData` `sRestoreLocalCollections` auf;
dort wird pro Sammlung versucht, den Handle aus IDB zu holen,
Permission anzufordern und neu zu mounten. Schlägt das fehl (keine
FSAA, kein Handle, Permission verweigert), wird ein Stub-Sprecher
angelegt; Klick darauf im Dropdown öffnet den Picker
(`sReloadStubCollection`). Sammlungen per `webkitdirectory` geladen
haben kein `handleId` und erscheinen nach JSON-Restore stets als Stub.

**Audio-Datei vs. Sätze vs. Geräusche (Buffer-Trennung):** `pFileBuf` (player.js) hält die vom User geladene Audiodatei; `sSentenceBuf` (sentences.js) den gerade dekodierten Satz; `pNoiseBuf` (state-side.js) den generierten oder geladenen Geräusch-Buffer. `pSourceBuf` ist eine Live-View auf den durch `pPlaybackMode` gewählten Slot (`"file"`, `"sentence"` oder `"noise"`), gesetzt über `pSetPlaybackMode(mode)`. Damit überschreibt die Sätze- oder Geräusch-Wiedergabe die Datei-Auswahl nicht mehr. `sStop()` setzt nach Sätze-Ende ebenfalls in den Datei-Modus zurück.

**Latenz-Kompensation (Inter-Ohr-Zeitversatz):** Zwischen `pGain` und `c.destination` hängt eine Verzögerungskette aus `latency.js`: `pGain → pLatSplitter → pLatDelayL / pLatDelayR → pLatMerger → c.destination`. `latInitGraph` wird einmalig aus `player.js` aufgerufen (nach `pGain`-Erstellung). `latSetSliderMs` steuert die Delay-Zeiten live während des Tests; `latApplyToPlayer` setzt den gespeicherten `latencyResult`-Wert (oder 0 wenn `plApplyLatency` aus). Während eines laufenden Latenz-Tests sitzt zusätzlich eine Balance-Stage zwischen `latTestSource` und `pGain` (Splitter + `latBalGainL`/`latBalGainR` + Merger). Die Gain-Werte kommen aus `getRawBalanceGains()` (ignoriert `plApplyBalance`) und werden beim Test-Start gesetzt und beim Stop verworfen.

**Inter-Ohr-Vergleich (Gesamtoffset L↔R):** `getPlayerBalance()`
in state-side.js berechnet den Mittelwert über alle gemessenen
`lrResults` (Stereo-Balance-Test) und liefert daraus ein
symmetrisches Stereo-Balance-Offset (positiv = rechts louder →
negativer Offset dämpft rechts ab). Wird im Player angewandt,
wenn die Checkbox „Stereo-Balance anwenden" (`plApplyBalance`) an
ist. Begrenzt auf ±60 dB. Die tatsächliche Gain-Verteilung auf
L/R erfolgt via `getPlayerBalanceGains()` unter Berücksichtigung
von `plBalanceMode` ("sym"/"left"/"right").

**Schieber-Tab-Modus und -Variante** (`lvTabMode`, `lvTabVariant`):
`lvTabMode` steuert den Anzeigemodus (`"rel"` = relativ ±dB,
`"abs"` = absolut in Hersteller-Einheit). `lvTabVariant` steuert die
Darstellungsvariante (`"stack"` = Diverging Stack — Default, `"sum"`
= nur Summenbalken, `"lines"` = Summenbalken + Vergleichslinien —
Radio ist im aktuellen Build per `display:none` ausgeblendet, Zeichen-
Code und Persistenz bleiben aktiv). Die Variante bleibt beim Modus-
Wechsel (rel ↔ abs) und beim MCL-Fallback **erhalten** — keine
automatische Anpassung. Beide Werte sind in JSON + localStorage
persistiert. `lvTabUpdateModeAvailability` prüft MCL-Verfügbarkeit
und graut ggf. den „absolut"-Radio aus; wird bei `lvTabRebuild` (Tab-
Wechsel, Side-Wechsel) und nach Datei-Laden aufgerufen.

**Absolutmodus-Präzision von `manualLevels`:** Im Absolutmodus
speichert `lvTabOnSchieberChange` die dB-Werte in `manualLevels[i]`
**ohne Rundung** (volle Float-Präzision). Bei hohem MCL ist ein
einzelner qu/CL/CU-Schritt eine sehr kleine dB-Änderung (z.B. +1 qu
bei MCL=200 ≈ 0.022 dB); jede Rundung auf 0.1 dB würde Schritte
schlucken und den Schieber blockieren. Im Relativmodus bleibt die
Rundung auf 0.1 dB. Anzeige-Rundung (z.B. `.toFixed(1)` im Draw-Pfad)
ist davon unabhängig.

**Levels-Tab Fokus-Modell:** `lvTabHasFocus` (in levels-tab.js) ist
nur dann `true`, wenn das Canvas (`#lvTabCv`) das fokussierte Element
ist. Die schwarze Umrahmung der aktuell aktiven Elektrode wird nur
gezeichnet, wenn `lvTabHasFocus === true`. Die globale Pfeiltasten-
Navigation (init.js Z. 887) reagiert nur, wenn das Canvas der
`document.activeElement` ist. `lvTabNavigableEl()` liefert die in der
aktuellen Modus-Konfiguration anwählbaren Elektroden: im Relativmodus
identisch zu `actEl()`, im Absolutmodus zusätzlich gefiltert auf
Elektroden mit eingetragenem MCL (`lvTabElHasMcl`). Klick + ←/→
springen daher im Absolutmodus über Elektroden ohne MCL hinweg.

**Tonart pro Test** (`toneType_test`, `toneType_balance`, `toneType_freqmatch`,
alle Default `richCiHF`) und **Tonfolge pro Test** (`sequence_test`,
`sequence_balance`, `sequence_freqmatch`, alle Default `"ab"`) sind seit BA 254
vollständig getrennt. Kein globaler `globalToneType`/`globalSequence` mehr.
Die Dropdowns für AB/ABA werden in `buildTestPanel` (test-ui.js) aus den
per-Test-Variablen initialisiert; bei Änderung schreibt der Event-Listener
nur in die Variable des jeweiligen Tests.

**Historische Kreuzverdrahtung der Tab-IDs:** `panel-levels` /
`tabLevels` gehört zum sichtbaren Tab „Kurven" (data-tab="levels",
Hauptmodul levels.js). `panel-schieber` / `tabSchieber` gehört zum
sichtbaren Tab „Schieber" (data-tab="schieber", Hauptmodul
levels-tab.js — i18n-Key `tabLevels` liefert den aktuellen Text
„Schieber"). Die DOM-IDs sind bewusst so belassen, um Diff-Aufwand zu
minimieren. Der i18n-Key `lvTabTitle` (Panel-Überschrift im Schieber-
Tab) und `plSrcLevels` (Player-Button-Label) liefern ebenfalls
„Schieber" / Sliders / Curseurs / Deslizadores.

**Player-Quellen-Toggles:** `plSrcMeas`, `plSrcLevels`, `plSrcCurves`
(alle in state-side.js, alle Default `true`). In `computeGains`
(player.js) werden drei unabhängige Summanden berechnet:
`plSrcMeas` → Messwerte aus `compWLS()`, `plSrcLevels` → nur
`manualLevels` (Schieber-Werte), `plSrcCurves` → nur
`getTotalPresetCurve()` (Preset-Anteil). Alle drei haben eigene
Buttons im Player-Panel.

**Preset-Berechnung** (`calcPresetCurve`, `getTotalPresetCurve`,
`getEffectiveLevels`) liegt in `levels.js`. `getEffectiveLevels`
wird nur noch von `expText` (file.js) genutzt; player.js greift
seit Bauanleitung 04 direkt auf `getTotalPresetCurve()` zu.
Die geometrischen Kurven (tilt, scurve, pivot, gauss) rechnen in
**log(Hz)** (Cent re 1000 Hz). Bei aktivem Frequenz-Warping
(Player-Toggle) rechnen diese Kurven auf den **gewarpten
Wahrnehmungs-Frequenzen** der Elektroden (via `effFreqDisplay`);
Bass-/High-Boost und Volume bleiben elektrodennummern-basiert.
`pr.center` wird als Hz-Wert
gespeichert (Default 1000 Hz), `pr.width` als Cent-Wert (Default
1200 ¢). JSON-Kennzeichen: `presetFormat: "freq-v3"`. Alte Dateien
ohne dieses Feld werden beim Laden automatisch migriert
(`_migratePresetsFromIndexToFreq` in file.js). Der
Migrations-Toast (`loadMigratedCurves`) erscheint nur, wenn
mindestens eine Seite migriert wurde **und** dort mindestens
eine Kurve vom Typ tilt/scurve/pivot/gauss mit `strength ≠ 0`
vorliegt (SII-only und Stärke-0-Kurven lösen keinen Toast aus).

**Levels → Player Live-Update:** `lvOnChange` in levels.js ruft am
Ende `pUpdEQ()` aus player.js auf. Außerdem ruft es `lvTabDraw()`
auf, damit der Schieber-Canvas synchron bleibt. Dadurch aktualisiert
sich der Player-Equalizer sofort, wenn manuelle Levels oder Presets
geändert werden.

**applyLang ruft modulübergreifend:** `updEqToggleBtn`,
`updBalApplyBtn` (beide tabs-eq.js), `updSideButtons` (state-side.js),
`buildFreqTable` (freq-table.js),
`buildImplantCard` (ui-implant.js), `renderResults` (results.js),
`renderFreqMatchResults` (results.js, nur wenn freqmatch-Sub-Tab aktiv),
`sUpdateUI` (sentences.js, aktualisiert Sätze-Card bei Sprachwechsel).
`applyLang` setzt auch alle `[data-t-opt]`-Elemente (Option-Labels in
Selects) über eine generische querySelectorAll-Schleife.

**Test-UI über Builder:** Die drei Sub-Tab-Hauptmodule (test.js,
lr-balance.js, freqmatch.js) erzeugen ihre UI nicht selbst, sondern
rufen `buildTestPanel(parentEl, cfg)` aus test-ui.js auf und greifen
auf das zurückgelieferte Element-Lookup-Objekt zu. UI-Änderungen am
einheitlichen Aufbau gehören in test-ui.js, nicht in die einzelnen
Test-Module.

**refEl-Wirkung:** `refEl` beeinflusst ausschließlich Anzeige und
Anwendung der Ergebnisse — konkret: WLS-Eichung in `compWLS`
(test.js), Highlight in `drawChart` (chart.js), Player-
Korrekturgewicht via `pUpdEQ` (player.js).
`refEl` hat **keine** Wirkung auf die Messung selbst (rohe
Vergleichspaare `bRes`). Das Dropdown zur Wahl der
Referenzelektrode sitzt im Ergebnis-Reiter (`#refEl`, befüllt
durch `updRef` in freq-table.js, Listener in init.js, triggert
`renderResults`/`drawLvChart`/`pUpdEQ`). In test.js wird `refEl`
nicht mehr beim Test-Start überschrieben.

Die Markierung der Referenzelektrode in den Loudness-Darstellungen
wird in `drawChart` (chart.js), `pDrawEQ` (player.js),
`_archivChartLoudness` und `_audiologChartImg` (beide in print-md.js)
als fettes schwarzes „Ref.-El."-Label am oberen Rand gezeichnet;
in den Loudness-Tabellen (`renderResults`, `_archivMdMeas`,
`_audiologLoudnessTable`) als neue Endspalte mit `X` in der
Referenzzeile. In Stereo-Balance-, Schieber- und
Frequenzabgleich-Darstellungen wird **nicht** markiert (kein
direkter `refEl`-Effekt im jeweiligen Inhalt).

**Tab-Sperre während Test:** sobald ein Test in einem der drei
Sub-Tabs läuft, sperrt `updateTabLockState` (tabs-eq.js) alle
Top-Level-Tabs **und** alle Sub-Tabs in Messungen außer dem aktiven.

**Tab/Subtab-Persistenz und Deep Linking:** `switchTab` (tabs-eq.js)
schreibt den aktiven Top-Level-Tab in `localStorage` unter
`ci-lb-activeTab` und setzt `location` per `history.pushState` auf
`#<tab>`. `switchSubtab` schreibt analog pro Parent unter
`ci-lb-subtab-<parent>` (nur für `messungen` und `ergebnisse`
relevant) und setzt den Hash auf `#<parent>:<subtab>`. Restore am
Ende des DOMContentLoaded-Handlers in `init.js`, nach allen anderen
Init-Schritten: URL-Hash hat Vorrang vor localStorage; abschließend
setzt `history.replaceState` den Hash auf den tatsächlich aktiven
Tab/Subtab (kein zusätzlicher History-Eintrag). Browser-Zurück/Vor
wird via `popstate`-Listener in tabs-eq.js abgefangen.

**DOM-Listener nicht nur in init.js:** Player und LR-Balance haben
eigene Top-Level-Listener bzw. eigene DOMContentLoaded-Handler in
ihren Modulen. test-ui.js verdrahtet die Test-spezifischen Listener
beim Aufruf von `buildTestPanel`. init.js ist die zentrale
Verdrahtung, aber nicht die einzige.

**Top-Level-Init am Ende von state-side.js:** Die Aufrufe
`initSideData("left", "medel")`, `initSideData("right", "medel")`,
`bindActiveSide()`, `updateMfrSelectLabels()` laufen beim Laden des
Moduls. Sie brauchen i18n und core, deshalb lädt state-side als
dritte Datei.

**Hersteller-Vergleiche verteilt:** `mfr === "medel"` /
`mfr === "ab"` / `mfr === "cochlear"` taucht in mehreren Modulen
auf, vor allem freq-table.js, ui-implant.js, core.js, levels.js.
Bei Hersteller-spezifischer Logik immer alle drei Module prüfen.

**i18n-Split:** Sprachdaten liegen jetzt in `i18n/de.js`, `i18n/en.js`, `i18n/fr.js`, `i18n/es.js`. Bei reinen Textänderungen reicht es, nur die betroffene Sprachdatei zu lesen (je ~700 Zeilen statt 2900).

**drawFreqMatchChart (chart.js):** Zeichnet das Diagramm im Sub-Tab
„Frequenzabgleich" unter Meßergebnisse. X-Achse: log-Hz der CI-Frequenz
(`varFreq`). Y-Achse: lineare Cent-Abweichung (positiv = subjektiv höher
als CI-Frequenz, negativ = tiefer). Null-Linie = perfekter Match.
Zusätzlich: hohle graue Kreise (aktiv, ungemessen) und vertikale graue
Striche mit „×" (deaktiviert) für CI-Elektroden ohne Meßwert.

**lrDrawChart (lr-balance.js):** Zeichnet das Balken-Diagramm im Ergebnis-
Sub-Tab „Stereo-Balance". Zeichnet alle Elektroden bis `count`: gemessene
als farbige Balken, aktive ungemessene als gestrichelter Strich mit
Querstrich-Symbol auf der Null-Linie, deaktivierte als hellgrauer voller
Balken mit „X"-Diagonale.

**Anzeige-Konvention für alle drei Ergebnis-Sub-Tabs** (deaktiviert =
grauer Balken/Strich + X, aktiv-ungemessen = gestrichelter Umriss/Kreis,
Status-Text per i18n) ist **nicht** über einen gemeinsamen Helper
implementiert, sondern dreifach separat: in `drawChart` (chart.js) für
Elektrodenlautstärke-Balance, in `lrDrawChart` + `lrRenderResults`
(lr-balance.js) für Stereo-Balance, und in `drawFreqMatchChart` +
`renderFreqMatchResults` (results.js) für Frequenzabgleich.

Der Deaktiviert-Balken (hellgrauer Vollbalken + X-Diagonale)
wird über den gemeinsamen Helper `drawDisabledBar` aus
`chart.js` gezeichnet und ist damit konsistent zwischen
`drawChart` und `lrDrawChart`. `drawFreqMatchChart` nutzt einen
eigenen Stil (Vertikallinie statt Balken) wegen der log-Hz-Achse.

**Historisches Relikt:** Der Tab mit `data-tab="setup"` wird mit dem
i18n-Key `tabFreq` ("Implantat") beschriftet. Setup war der alte Name,
Implantat ist der aktuelle UI-Text. Wer nach „Frequenzen" sucht,
findet im aktuellen Build nichts mit diesem Namen.

**Globaler Dateinamen-Suffix:** Das Eingabefeld `#userFileSuffix` im
Laden/Speichern-Tab speichert seinen Wert in `userFileSuffix`
(state-side.js) und in `localStorage` (`ci-lb-userFileSuffix`, sofort
bei jedem `input`-Event). Beim Erzeugen von Download-Dateinamen wird
der Wert über `_applyUserFileSuffix` (file.js) zwischen Basisname und
Endung eingeschoben — wirkt in `saveJson`, `exportEasyEffects`,
`mdArchivFilename`, `mdAudiologFilename`.

**Markdown-Export (Archiv-Box):** `collectArchivData` in
print-md.js liest einmal alle relevanten State-Variablen ein
(`sideData`, `lrResults`, `latencyResult`, `fRes`, alle
`plApply*`/`plBalanceMode`/`pMaplaw*`/`pWarp*`-Werte sowie
globale Test-/Levels-Tab-Werte) und liefert ein strukturiertes
Objekt. `renderArchivMarkdown(data)` rendert daraus den
Markdown-Bericht. Pro Seite wird `withSide(side, fn)` benutzt,
um die seiten-spezifische Live-View korrekt zu binden. Die
Aktionen „Markdown Text exportieren" und „Bericht drucken"
sitzen in der Archiv-Karte (`#cardArchiv`). Druck (`fPrintBtn`)
ruft `renderArchivPrintHtml(collectArchivData())`, wandelt den
Markdown über `_mdToHtmlBasic` in HTML und hängt PNG-Grafiken
pro Sektion ein (Messungen-Loudness, Schieber, Kurven,
Frequenzabgleich, Stereo-Balance, Player-EQ — pro Seite je
nach `*.has`-Gate). Der alte DOM-basierte Inline-Handler in
init.js ist entfernt.

**Audiologen-Auftrag (Modus B):** `buildAudiologMarkdown` in
print-md.js erzeugt einen strukturierten Korrektur-Bericht.
Reihenfolge: Kopf (Datum, Side, Tool-Version), Testprogramm-Hinweis
(falls erkannt), EQ-aus-Hinweis (falls EQ aus). Dann der bilaterale
Block — Sektionen, die beide Seiten gleichermaßen betreffen, in fester
Reihenfolge: Stereo-Balance, Latenz, Hinweise an den Audiologen,
Fehlende Implantat-Angaben (mit italic Einleitungssatz aus i18n-Key
`audiologMissingIntro`). Anschließend die Pro-Seite-Blöcke — erst
LINKS komplett, dann RECHTS komplett, kein Vermischen: Seiten-H2 mit
Meta-Zeile, dann H3 Lautstärken-Korrektur, H3 MAPLAW-Änderung (wenn
applikabel), H3 Änderung der Mittenfrequenzen (wenn applikabel).
`_audiologMaplawSection(mainSides, headerLevel)` unterstützt einen
optionalen Header-Level-Parameter (Default "##"); aus dem Pro-Seite-
Loop wird sie mit "###" aufgerufen, damit MAPLAW als H3 unter der
Seiten-H2 erscheint. Druck-Pfad (`audiologPrint`): zwei Chart-Injektionen mit je laufendem
`searchFrom`-Offset (bilateral-korrekt; Reihenfolge der Seiten-Blöcke
entspricht `mainSides`): ΔdB-Bar-Chart **vor** `<h3>audiologSecLoudness</h3>`
und — wenn `pWarpOn` — Frequenzabgleich-Graph (`_audiologFreqChartImg`)
**nach** `<h3>audiologSecFreq</h3>`. Bei sym-Warp + einseitigem Druck
(`pWarpMode === "symmetric"` && `mainSides.length === 1`) werden statt
einer zwei H3-Sektionen erzeugt — beide mit Side-Suffix
(`<h3>audiologSecFreq — ${sideLbl}</h3>`), eine für die gedruckte Seite,
eine für die andere; der Injektor sucht und befüllt entsprechend zwei
Marker pro mainSide. `_audiologFreqTable(side)` und `_audiologFreqChartImg(side)`
rendern nur die jeweils übergebene Seite (kein Misch-Modus mehr; die
sym-Doppelung lebt allein im Generator/Injektor). Druck nutzt
`openPrintWindow` aus print.js.

## Edit-Szenarien

### Reine Textänderung (Übersetzung)
- i18n.js: betroffene Schlüssel im L-Objekt aller 4 Sprachen anpassen

### Neue Sprache hinzufügen
- i18n.js: neuer Sprach-Block im L-Objekt
- index.html: neue `<option>` im langSelect-Dropdown

### Neue Hersteller-Daten (z.B. Modell zu MED-EL ergänzen)
- core.js: IMPLANTS / PROCESSORS-Eintrag

### Neuer Hersteller (4. Hersteller)
Großer Edit, mehrere Module:
- core.js: IMPLANTS, PROCESSORS, MFR-Eintrag, neue calc*-Funktion
- index.html: neue `<option value="...">` im mfrSelect
- i18n.js: Strings für Hersteller-Namen und Felder
- freq-table.js: switchMfr-Logik, ggf. isXxx-Flags
- ui-implant.js: buildImplantCard (Hersteller-spezifische Felder)
- file.js: Hersteller-Vergleich in saveJson/applyLoadedData prüfen
- state-side.js: initSideData prüfen

### Neue Preset-Art (z.B. „Notch-Filter")
- core.js: PR_TYPES erweitern, PR_NAMES, PR_EXPL ergänzen, ggf.
  PR_HAS_CENTER / PR_HAS_WIDTH / PR_HAS_CUTOFF
- levels.js: calcPresetCurve um neuen Typ ergänzen, buildPrTbl paßt
  sich automatisch an PR_*-Konstanten an
- state-side.js: initPresets prüfen (Default-Werte für neuen Typ)
- i18n.js: Strings für PR_NAMES und PR_EXPL

### UI eines Tabs ändern
- index.html: HTML-Struktur des Panels
- Hauptmodul des Tabs (siehe Tabelle oben)
- evtl. i18n.js: neue Strings
- evtl. init.js: Event-Listener-Verdrahtung

### Aufbau eines Test-Sub-Tabs ändern
- **test-ui.js** ist der zentrale Ort. Änderungen am einheitlichen
  Aufbau (zusätzliche Zeile, geänderte Hinweisbox, neue Hook) gehören
  hierher.
- Anschließend Configs in test.js, lr-balance.js, freqmatch.js
  prüfen, ob neue Felder gesetzt werden müssen.

### Globaler State hinzufügen
- state-side.js: Variable deklarieren, Default setzen
- file.js: saveJson + applyLoadedData für Persistenz erweitern
- init.js: Autosave-Block für localStorage prüfen
- CODESTRUKTUR.md: in der state-side.js-Zeile mit aufnehmen

### Neuer Tab-Einzeldruck
- Tab-Modul oder init.js: Druck-Knopf-Listener registrieren
- print.js: nicht ändern (Infrastruktur ist stabil)
- Bei neuen Druck-Header-Feldern: i18n.js (alle 4 Sprachen),
  buildPrintHeader in print.js erweitern

### Neue JS-Datei hinzufügen oder bestehende entfernen
- index.html: `<script>`-Tag an passender Position einfügen oder
  entfernen
- **CODESTRUKTUR.md: Modul-Tabelle aktualisieren** (Nummerierung,
  Inhaltsbeschreibung, Querverweise im Datenfluss-Block)

### Neuer Event-Listener
- init.js (zentral) ODER direkt im jeweiligen Modul (siehe Player
  und LR-Balance als Vorbild)

### Markdown-Generator erweitern oder anpassen
- `print-md.js`: Datensammler `collectArchivData` um neue Felder
  erweitern, passenden `_archivMd*`-Sektion-Helfer hinzufügen
  und in `renderArchivMarkdown` einbinden.
- `i18n.js`: neue `archiv…`-Keys in allen vier Sprachen.
- Bei Verweis auf neue Tool-State-Variablen: deren Modul muß
  **vor** `print-md.js` im Loader stehen.

## Skripte und Hilfswerkzeuge

### scripts/fetch_common_voice.py

Pre-Fetch-Werkzeug für Common Voice 17.0 via `fsicoli/common_voice_17_0`
auf Hugging Face. Streamt den `train`-Split, filtert auf
Sprecher-Diversität und Wortlänge, schreibt kuratierte MP3-Auswahl nach
`assets/sentences/cv-<lang>/`. Wird lokal/im Codespace mit
`python scripts/fetch_common_voice.py --lang <iso> --count <n>`
aufgerufen. Lizenz der erzeugten Daten: CC0-1.0.

### scripts/build_embed.py

Erzeugt aus `assets/sentences/sentences.json` und den vorhandenen
MP3s pro Sprache eine kleine Lazy-Load-Datei
`assets/sentences/embed/<lang>.js` für den Offline-Modus
(5 Recordings pro Sprache, Audio als data:-URL). Lokal aufrufen nach
Korpus-Aktualisierung: `python3 scripts/build_embed.py`.

### scripts/build_manifests.py

Erzeugt die Manifest-Dateien unter `audio.manifest/` aus lokalen
Voice-Mirror-Verzeichnissen. Unterstützt Subkommandos `--only <corpus>`
für gezielte Teilbuilds. Erstellt nach einem vollständigen Lauf einen
Differenzen-Report `audio.manifest/_diff_report.md`, der fehlende
Audiodateien pro Corpus auflistet. Hauptkategorien: ARU Speech Corpus,
Common Voice, MUSAN, Librivox u. a.

### scripts/aru_ascii_rename.py

Benennt die Dateien im ARU-Mirror
(`voice/opus/ARU_Speech_Corpus_v1_0/`) von Leerzeichen- und
`=`-haltigen Originalnamen (`ID01_ARU_Fs=65536Hz_Standard speech - List 1 - Sentence 1 - Version 1_0.opus`)
auf ASCII-sichere URLs um (`id01-L01-S01-v1.opus`). Aktualisiert
anschließend die Sidecar-Datei `sentences_ARU.txt`, damit
`build_manifests.py` beim nächsten Lauf die neuen Namen findet.
Idempotent. Aufruf: `python3 scripts/aru_ascii_rename.py [--dry-run]
[--mirror <pfad>]`.

### Sprachdatensätze/ARU_Speech_Corpus_v1_0/sentences_ARU.txt

Manifest-Datei für den ARU Speech Corpus (12 Sprecher ID01–ID12,
72 Listen × 10 Sätze = 720 Texte, 8640 Einträge gesamt). Format:
`<dateiname.wav>: <satztext>` — Colon-Separator, Dateinamen enthalten
Leerzeichen. Wird vom generischen Manifest-Parser in `sentences.js`
(`sParseGenericManifest`, dritte Regex-Variante) automatisch erkannt,
sobald der ARU-Ordner in den Satzplayer hochgeladen wird. Die Datei
liegt direkt im Corpus-Ordner; der Browser findet sie beim
Ordner-Upload automatisch. Erzeugt mit `pypdf` aus dem beigelegten
`IEEE_wordlists.pdf` (Python-Snippet in den Commit-Kommentaren).

## Strukturelle Eigenschaften

- Kein ES-Module-System, kein import/export
- Kein IIFE-Wrapping. Alles globaler Scope
- Keine Typprüfung, kein Lint-Setup
- Build oder Bundling gibt es nicht – Browser lädt alle Module einzeln
- Keine automatisierten Tests; Prüfung manuell im Browser mit DevTools

Archiv-Ordner `archive/debug-tests/` (ab BA 83): hält
archivierte Bau-Diagnose-Tests aus abgenommenen Bauanleitungen.
Nicht Teil des aktiven Codes — wird vom Loader **nicht** geholt.
Reaktivierung via Zurück-Kopieren nach `js/debug-tests-current.js`.
